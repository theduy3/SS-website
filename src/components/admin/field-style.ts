// Shared Tailwind styling token for admin form inputs. Extracted from PopupForm
// so PopupForm and LocalizedGroup use one class string instead of duplicating it
// (or LocalizedGroup back-importing from its former parent).

export const inputClass =
  "w-full rounded-lg border border-tan bg-beige px-3 py-2 text-sm outline-none focus:border-espresso";
