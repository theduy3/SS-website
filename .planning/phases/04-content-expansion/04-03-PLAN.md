---
phase: 04-content-expansion
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/lib/guides.ts
  - src/dictionaries/en.json
  - src/dictionaries/fr.json
  - src/dictionaries/es.json
  - src/dictionaries/ar.json
  - src/app/[lang]/guides/[slug]/page.tsx
autonomous: false
requirements: [EXP-02]
must_haves:
  truths:
    - "/guides/[slug] resolves a localized slug → renders a guide page; wrong-locale slug 404s"
    - "Each guide opens with a direct answer block as a bare SSR <p> (no Reveal)"
    - "Each guide emits valid Article + Breadcrumb JSON-LD"
    - "The answer block renders in all 4 locales (en/fr/es/ar)"
    - "KeyPageChrome mounts exactly ONCE per guide page"
    - "The cost guide uses [PRICE:*] tokens that the build gate refuses to ship unfilled"
  artifacts:
    - path: "src/lib/guides.ts"
      provides: "Guide registry mirroring comparisons.ts (registry, slugParams, bySlug, path, pathsByLocale, guidesForService)"
      contains: "export function guidesForService"
    - path: "src/app/[lang]/guides/[slug]/page.tsx"
      provides: "/guides/[slug] route with Article/Breadcrumb + KeyPageChrome"
      contains: "articleGraph"
    - path: "src/dictionaries/en.json"
      provides: "3 guide content blocks (cost/care/best-for)"
      contains: "manicure-cost-laval"
  key_links:
    - from: "src/app/[lang]/guides/[slug]/page.tsx"
      to: "src/lib/guides.ts"
      via: "guideBySlug resolves localized slug → dict.guides[id]"
      pattern: "guideBySlug"
    - from: "src/app/[lang]/guides/[slug]/page.tsx"
      to: "src/lib/seo.ts"
      via: "articleGraph + breadcrumbGraph emitted via JsonLd"
      pattern: "articleGraph"
  prohibitions:
    - "No answer block behind Reveal/opacity:0 (crawler-visibility invariant, EXP-02)"
    - "No /guides route in STANDALONE_PATHS — these are localized, not standalone (D-10)"
    - "No HowTo schema on guides — Article + Breadcrumb only (D-08)"
    - "No unfilled [PRICE:*] token may ship — cost guide tokens gated by Plan 01's test (D-14)"
    - "No second TrustBand — mount KeyPageChrome ONCE (guard 6a4295e)"
    - "Best-for guide recommends per-service by look/longevity, NOT a forced bridal bundle (D-06)"
---

<objective>
Deliver the guide set (EXP-02): a new `guides.ts` registry mirroring `comparisons.ts`, a new localized `/guides/[slug]` route, and 3 guides (cost / care / best-for) with answer-first blocks, Article/Breadcrumb JSON-LD, KeyPageChrome, and 4-locale content. The cost guide uses `[PRICE:*]` tokens ("Starting at $X", D-07) that Plan 01's gate refuses to ship unfilled.

Purpose: EXP-02 extends the answer-first surface to guide content on a parallel route. The registry, route, and schema all have exact analogs (comparisons.ts / comparison route / breadcrumbGraph) — this is mirror-and-fill.
Output: guides.ts, the /guides/[slug] route, 3 guides in 4 locales, Article/Breadcrumb schema, KeyPageChrome mounted once.
</objective>

<execution_context>
@/Users/theduy/Repo/SS-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/theduy/Repo/SS-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-expansion/04-CONTEXT.md
@.planning/phases/04-content-expansion/04-PATTERNS.md
@.planning/phases/04-content-expansion/04-UI-SPEC.md
@.planning/phases/04-content-expansion/04-01-SUMMARY.md
@.planning/phases/04-content-expansion/04-02-SUMMARY.md

@src/lib/comparisons.ts
@src/app/[lang]/comparisons/[slug]/page.tsx
@src/lib/services.ts
@src/components/KeyPageChrome.tsx
@src/lib/consent.server.ts
@src/lib/dictionary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guides.ts registry mirroring comparisons.ts</name>
  <files>src/lib/guides.ts</files>
  <read_first>
    - src/lib/comparisons.ts (full — the exact template: type, registry, comparisonSlugParams/BySlug/Path/PathsByLocale/comparisonsForService)
    - src/lib/services.ts lines 11 (ServiceId), 77-90 (localized-slug convention)
    - .planning/phases/04-content-expansion/04-CONTEXT.md D-05/D-09 (3 guides + service mappings; localized per-locale slugs)
    - .planning/phases/04-content-expansion/04-PATTERNS.md "src/lib/guides.ts" section (exact substitution map)
  </read_first>
  <action>
    Create src/lib/guides.ts as a structural mirror of comparisons.ts. Define GuideId union of the 3 guide ids (manicure-cost-laval → service manicure; gel-manicure-care → service manicure; best-nails-wedding → service whichever maps best per D-06, e.g. manicure or lash-extensions). Define `Guide` type ({ id, slug: Record<Locale,string>, service: ServiceId }). Export `guides` readonly array with localized per-locale slugs (factual, NAP-consistent). Export the 6 helpers renamed: guideSlugParams, guideBySlug, guidePath (prefix `/guides/`), guidePathsByLocale, guidesForService. Mirror the comparisons helper bodies exactly — only names + the `/guides/` prefix change.
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>guides.ts exports the registry + all 6 helpers; GuideId has 3 members; tsc surfaces only the expected missing-en.json-guides-key error (resolved Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Author 4-locale guide content (EN-first, D-13) with [PRICE:*] tokens</name>
  <files>src/dictionaries/en.json, src/dictionaries/fr.json, src/dictionaries/es.json, src/dictionaries/ar.json</files>
  <read_first>
    - src/dictionaries/en.json lines 969-1186 (comparisons block — analog for the new top-level `guides` key shape)
    - src/lib/dictionary.ts (Dictionary = typeof en — the new `guides` key MUST exist in en.json for the route to typecheck)
    - .planning/phases/04-content-expansion/04-CONTEXT.md D-05/D-06/D-07 (cost="Starting at $X" via [PRICE:*]; best-for=per-service by look/longevity, NOT a bundle; care=make gel last longer)
    - .planning/phases/04-content-expansion/04-PATTERNS.md dictionaries "guides" key section (answer/sections/faq shape)
    - .planning/phases/04-content-expansion/04-01-SUMMARY.md ([PRICE:*] gate is already live — tokens left unfilled will fail the build, by design)
  </read_first>
  <action>
    EN-FIRST (D-13). Step A: add a NEW top-level `guides` key to en.json (parallel to `comparisons`) with 3 blocks. Each block: title, metaTitle, metaDescription, an `answer` string holding the 40-60 word answer-first block, `sections`, and `faq` — exact sub-keys at your discretion but they MUST match what the route template (Task 3) reads. Cost guide answer uses "Starting at $[PRICE:manicure]" tokens (D-07/D-14) — write the tokens, do NOT invent prices; the user fills real Sans Souci numbers before merge and the Plan 01 gate blocks an unfilled token. Best-for guide recommends per-service by look/longevity with multiple booking entry points, NOT a forced bridal bundle (D-06). Step B (translation, explicit): mirror all 3 blocks into fr/es/ar with identical keys; KEEP the `[PRICE:*]` tokens verbatim in every locale (the gate scans all four). PAUSE for EN review (D-13) at the checkpoint below before translating.
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>All 4 dictionaries carry a `guides` key with 3 identical-keyed blocks; cost guide carries [PRICE:*] tokens in all locales; tsc passes. (price-token gate is RED here by design until the user fills tokens — that is the fail-loud behavior, not a regression.)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Review EN guide copy before fr/es/ar translation (D-13)</name>
  <files>src/dictionaries/en.json</files>
  <action>HALT after EN authoring (Task 2 Step A), before translation (Step B). Present the new EN `guides` block to the user. What was built: EN copy for the 3 guides — cost (with price tokens), care, best-for. How to verify: (1) review the new `guides` block in src/dictionaries/en.json; (2) confirm each `answer` is answer-first and factual, best-for is per-service (not a bundle), care covers gel-longevity tips; (3) confirm the cost guide uses price tokens, not invented dollar figures. Approve EN before the agent translates to fr/es/ar (D-13). Note: the real price numbers are filled by the user before merge — the build gate stays red until then by design.</action>
  <verify><human-check>User has reviewed the EN guide copy and replied "approved" (or supplied edits, applied before translation).</human-check></verify>
  <done>User approved the EN copy; fr/es/ar translation may proceed. Resume signal: user types "approved" or describes edits.</done>
</task>

<task type="auto">
  <name>Task 3: Create the /guides/[slug] route with Article/Breadcrumb + KeyPageChrome</name>
  <files>src/app/[lang]/guides/[slug]/page.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-static-params.md AND node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md (NON-STANDARD Next.js per AGENTS.md — params is a Promise, generateStaticParams receives params as a prop; verify route signatures against installed docs before writing)
    - src/app/[lang]/comparisons/[slug]/page.tsx (full — the route template AFTER Plan 02's retrofit: generateStaticParams/Metadata, KeyPageChrome mount, answer-first bare <p>, breadcrumbGraph, getDictionary import path `../../dictionaries`)
    - src/lib/guides.ts (Task 1 — guideBySlug/guideSlugParams/guidePath/guidePathsByLocale)
    - .planning/phases/04-content-expansion/04-01-SUMMARY.md (articleGraph signature)
    - .planning/phases/04-content-expansion/04-PATTERNS.md "guides route" section + "Answer-first SSR invariant" + "KeyPageChrome mount"
  </read_first>
  <action>
    Create src/app/[lang]/guides/[slug]/page.tsx mirroring the comparison route. generateStaticParams returns guideSlugParams(params.lang) behind an isLocale guard; generateMetadata uses pageMetadata(lang, guidePath(g,lang), {title, description, routeByLocale: guidePathsByLocale(g)}). Page component: await params, isLocale guard → notFound, guideBySlug → notFound, getDictionary, read dict.guides[g.id]. Emit `<JsonLd data={articleGraph(lang, {name: g.title, description: g.metaDescription, path: guidePath(g,lang)})} />` and `breadcrumbGraph(lang, [Home → Guides → {title}])` — Article + Breadcrumb only, NO HowTo (D-08). Render the `answer` block as a BARE `<p className="...text-lg leading-relaxed text-mocha">` (no Reveal, crawler-visibility invariant); sections/FAQ/CTA may use Reveal. Mount `<KeyPageChrome locale={lang} dict={dict} consentKnown={consentKnown} />` ONCE (readConsent pattern), no showTrustBand={false}, never in layout. Each guide links to its related service money-page + book CTA (D-12). Do NOT add this route to STANDALONE_PATHS (D-10 — localized route).
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>/guides/[slug] route exists, emits Article+Breadcrumb (no HowTo), answer block is a bare SSR <p>, KeyPageChrome once, not in STANDALONE_PATHS; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide dictionary copy → SSR HTML/JSON-LD | answer + sections + Article schema rendered into the page |
| [PRICE:*] token → production | unfilled price token must fail the build (Plan 01 gate) |
| localized slug → guideBySlug | wrong-locale slug must 404 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering (XSS) | guide route JSON-LD | mitigate | articleGraph renders through the existing JsonLd `<` escape path; no new sink |
| T-04-08 | Tampering (stale price) | cost guide [PRICE:*] | mitigate | Plan 01 build-fail gate blocks any unfilled token at `bun run test` (D-14) |
| T-04-09 | Information disclosure | guide route 404 path | accept | isLocale + guideBySlug guards 404 cleanly; static content, no sensitive data |
| T-04-SC | Tampering | npm installs | n/a | No package installs this plan |
</threat_model>

<verification>
- After build, `curl -s http://localhost:3000/en/guides/<en-slug> | grep -i "<answer opening words>"` shows the answer in raw SSR HTML; repeat fr/es/ar.
- `curl ... | grep '"@type":"Article"'` and `'"@type":"BreadcrumbList"'` present; `grep -c '"@type":"HowTo"'` is 0.
- After the user fills real prices, `bun run test` (price gate) passes; before that it is RED by design.
- `bunx tsc --noEmit` passes; grep confirms `/guides` is NOT in src/proxy.ts STANDALONE_PATHS.
</verification>

<success_criteria>
3 guides render answer-first (bare SSR <p>) in all 4 locales on /guides/[slug], emit Article/Breadcrumb (no HowTo), mount KeyPageChrome once, link to related service + book CTA, and carry [PRICE:*] tokens the build gate enforces. Route is localized (not standalone).
</success_criteria>

<output>
Create `.planning/phases/04-content-expansion/04-03-SUMMARY.md` when done — list the 3 guide ids + per-locale slugs and the guidesForService export (Plan 04 needs them).
</output>
