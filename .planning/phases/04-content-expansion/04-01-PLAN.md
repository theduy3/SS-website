---
phase: 04-content-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo.ts
  - src/components/JsonLd.tsx
  - src/lib/seo.test.ts
  - src/lib/price-tokens.test.ts
autonomous: true
requirements: [EXP-01, EXP-02]
must_haves:
  truths:
    - "productGraph emits a valid schema.org Product node with @id-linked brand"
    - "articleGraph emits a valid schema.org Article node with inLanguage"
    - "reviewGraph emits NOTHING (null) when reviewsFetchedAt is falsy"
    - "A [PRICE:*] token anywhere in any dictionary fails `bun run test`"
  artifacts:
    - path: "src/lib/seo.ts"
      provides: "productGraph, reviewGraph, articleGraph builders"
      contains: "export function productGraph"
    - path: "src/lib/price-tokens.test.ts"
      provides: "[PRICE:*] build-fail gate (D-14)"
      contains: "PRICE"
  key_links:
    - from: "src/lib/seo.ts"
      to: "src/lib/reviews.ts"
      via: "reviewGraph gates on reviewsFetchedAt import (SCHEMA-03)"
      pattern: "reviewsFetchedAt"
    - from: "src/components/JsonLd.tsx"
      to: "src/lib/seo.ts"
      via: "JsonLd no-ops when a gated builder (reviewGraph) returns null"
      pattern: "data ==? null|!data|if \\(!data\\)"
  prohibitions:
    - "No self-made Review/aggregateRating without the reviewsFetchedAt live-data gate (D-04, SCHEMA-03)"
    - "No new unescaped JSON-LD sink — all schema renders through the existing JsonLd \\u003c escape path"
---

<objective>
Add the three net-new JSON-LD builders that Plans 02 and 03 depend on (`productGraph`, `reviewGraph`, `articleGraph`), make `JsonLd` no-op safely on `null`, and install the `[PRICE:*]` build-fail gate (D-14) BEFORE any price tokens are authored — so CI blocks any unfilled token from the moment content work begins.

Purpose: These are the shared schema + safety dependencies for both content sets. Building them first (Wave 1) unblocks the comparison retrofit (Plan 02) and the guide route (Plan 03) and guarantees the fail-loud price gate exists before tokens are written.
Output: 3 new builders in `seo.ts`, a null-safe `JsonLd`, the `[PRICE:*]` vitest gate, and unit tests for the builders + the review gate.
</objective>

<execution_context>
@/Users/theduy/Repo/SS-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/theduy/Repo/SS-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-expansion/04-CONTEXT.md
@.planning/phases/04-content-expansion/04-PATTERNS.md

@src/lib/seo.ts
@src/components/JsonLd.tsx
@src/lib/reviews.ts
@src/lib/seo.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add productGraph, reviewGraph (gated), articleGraph to seo.ts + null-safe JsonLd</name>
  <files>src/lib/seo.ts, src/components/JsonLd.tsx, src/lib/seo.test.ts</files>
  <read_first>
    - src/lib/seo.ts lines 110, 135-200, 283-300 (BUSINESS_ID const, organizationGraph reviewsFetchedAt gate spread, breadcrumbGraph builder shape to mirror)
    - src/lib/reviews.ts (reviewsFetchedAt is `string | null` — falsy = no live review data; gate on it)
    - src/components/JsonLd.tsx (current signature `data: object`; the < escape is the ONLY sanctioned JSON-LD sink — do NOT add a new one)
    - src/lib/seo.test.ts (existing vitest + builder-assertion patterns to mirror; run with `bun run test`)
    - .planning/phases/04-content-expansion/04-PATTERNS.md "src/lib/seo.ts" section (exact builder bodies) + "reviewsFetchedAt live-data gate" shared pattern
  </read_first>
  <behavior>
    - productGraph(lang, {name, description, path}) → object with @context "https://schema.org", @type "Product", name, description, url = `${site.url}/${lang}${path}`, brand: { "@id": BUSINESS_ID }.
    - articleGraph(lang, {name, description, path}) → @type "Article", headline=name, description, url=`${site.url}/${lang}${path}`, publisher: { "@id": BUSINESS_ID }, inLanguage: lang.
    - reviewGraph(lang) → returns null when reviewsFetchedAt is falsy (SCHEMA-03 gate); when truthy, returns a Review node itemReviewed @id BUSINESS_ID with reviewRating from site.reviews. Current fixture has reviewsFetchedAt falsy → reviewGraph MUST return null in the default test.
    - JsonLd({data}) when data is null/undefined renders nothing (no <script> tag emitted), so `<JsonLd data={reviewGraph(lang)} />` is safe to mount unconditionally.
  </behavior>
  <action>
    Add three exported builders to src/lib/seo.ts mirroring the breadcrumbGraph parameter style (PATTERNS.md gives exact bodies). All three must reference the existing BUSINESS_ID const and `site.url` for absolute JSON-LD URLs — do NOT recompute the origin. reviewGraph must gate on the existing `reviewsFetchedAt` import exactly like organizationGraph's aggregateRating spread (D-04, SCHEMA-03): return null when falsy, never emit a self-authored rating. Widen JsonLd's prop type to `object | null | undefined` and early-return null when data is falsy, so a gated builder mounts safely. Keep the existing `.replace(/</g, "\\u003c")` escape — this is the only sanctioned JSON-LD sink (FOUND-01/D-10); do not introduce a second serialization path. Extend src/lib/seo.test.ts with assertions: productGraph/articleGraph shape (@type, url, @id-linked brand/publisher, inLanguage for article), and reviewGraph returns null under the current (falsy reviewsFetchedAt) fixture.
  </action>
  <verify>
    <automated>bun run test -- src/lib/seo.test.ts</automated>
  </verify>
  <done>productGraph/articleGraph return correctly-shaped nodes; reviewGraph returns null with the current falsy reviewsFetchedAt; JsonLd renders nothing for null; `bun run test -- src/lib/seo.test.ts` passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install the [PRICE:*] build-fail gate (D-14)</name>
  <files>src/lib/price-tokens.test.ts</files>
  <read_first>
    - .planning/phases/04-content-expansion/04-PATTERNS.md "Vitest test pattern for [PRICE:*] build-fail gate" (exact test body)
    - src/lib/seo.test.ts (vitest import style + how dictionaries are imported in tests)
    - Project memory: test runner is Vitest — `bun run test`, NEVER `bun test`
  </read_first>
  <behavior>
    - Test scans the concatenated JSON of all four dictionaries (en/fr/es/ar).
    - PASS today (no tokens exist yet).
    - FAIL the moment any dictionary value contains an unfilled price token, so Plans 02/03 can safely author tokens and the build refuses to ship them unfilled.
  </behavior>
  <action>
    Create src/lib/price-tokens.test.ts that imports all four dictionary JSONs, stringifies them, and asserts the combined text does NOT contain an unfilled price token. Match the token by its conceptual pattern (a bracketed PRICE marker with a key), not by pasting the literal sentinel into a string that the file would then self-match — express the matcher as a RegExp built from `PRICE` so this very test file does not trip its own gate. This file must be committed in this wave (before any tokens are written) so CI blocks unfilled tokens at merge (Rule 12: fail loud).
  </action>
  <verify>
    <automated>bun run test -- src/lib/price-tokens.test.ts</automated>
  </verify>
  <done>Gate passes on the current token-free dictionaries; a deliberately-inserted unfilled token makes it fail (spot-check by temporary insert, then revert). Committed this wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder output → HTML `<script type=ld+json>` | schema data is serialized into the SSR page; a `</script>` in any field could break out |
| dictionary content → production build | unfilled `[PRICE:*]` tokens could ship as visible broken copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (XSS) | JsonLd / new seo.ts builders | mitigate | All 3 builders render through the existing JsonLd `<` escape path (D-10); no new serialization sink introduced |
| T-04-02 | Information disclosure | reviewGraph | mitigate | Gated on `reviewsFetchedAt` (SCHEMA-03); returns null — never emits a self-authored/placeholder rating (D-04) |
| T-04-03 | Tampering (stale data) | dictionaries | mitigate | `[PRICE:*]` build-fail gate (D-14) fails `bun run test` on any unfilled token |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (pure source changes); RESEARCH package-audit not required |
</threat_model>

<verification>
- `bun run test -- src/lib/seo.test.ts` and `bun run test -- src/lib/price-tokens.test.ts` both pass.
- `bunx tsc --noEmit` (or project typecheck) passes with the widened JsonLd type.
- Grep confirms reviewGraph references `reviewsFetchedAt` and all three builders reference `BUSINESS_ID`.
</verification>

<success_criteria>
3 builders exist and are type-correct; reviewGraph is null under the current fixture; JsonLd no-ops on null; the `[PRICE:*]` gate is committed and green. Downstream plans can import all three builders.
</success_criteria>

<output>
Create `.planning/phases/04-content-expansion/04-01-SUMMARY.md` when done — list the exported builder signatures and the gate test path for Plans 02/03 to import.
</output>
