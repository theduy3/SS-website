---
phase: 04-content-expansion
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/comparisons.ts
  - src/dictionaries/en.json
  - src/dictionaries/fr.json
  - src/dictionaries/es.json
  - src/dictionaries/ar.json
  - src/app/[lang]/comparisons/[slug]/page.tsx
autonomous: false
requirements: [EXP-01]
must_haves:
  truths:
    - "Each of the 6 comparison pages opens with a 40-60 word answer-first verdict as a bare SSR <p> (no Reveal)"
    - "Each comparison page renders a scannable ComparisonTable in raw SSR HTML"
    - "Each comparison page emits valid Product + Breadcrumb JSON-LD (and Review only when gated data exists)"
    - "The answer-first verdict renders in all 4 locales (en/fr/es/ar)"
    - "KeyPageChrome (TrustBand + StickyCtaBar) mounts exactly ONCE per comparison page (D-11)"
    - "The TrustBand/rating bar appears exactly once per comparison page in SSR HTML — `curl <page> | grep -c '20+ years experience'` equals 1, never 2 (TrustBand's trust.established marker; guard the 6a4295e double-TrustBand bug) (D-11)"
  artifacts:
    - path: "src/lib/comparisons.ts"
      provides: "6 comparison entries (3 existing + 3 category-alternative)"
      contains: "salon-gel-vs-diy-kit"
    - path: "src/app/[lang]/comparisons/[slug]/page.tsx"
      provides: "answer-first retrofit + Product/Review/Breadcrumb schema + KeyPageChrome"
      contains: "productGraph"
    - path: "src/dictionaries/en.json"
      provides: "6 comparison content blocks (verdict/table/faq)"
      contains: "salon-gel-vs-diy-kit"
  key_links:
    - from: "src/app/[lang]/comparisons/[slug]/page.tsx"
      to: "src/lib/seo.ts"
      via: "productGraph/reviewGraph/breadcrumbGraph emitted via JsonLd"
      pattern: "productGraph|reviewGraph"
    - from: "src/app/[lang]/comparisons/[slug]/page.tsx"
      to: "src/lib/comparisons.ts"
      via: "comparisonBySlug resolves localized slug → dict.comparisons[id]"
      pattern: "comparisonBySlug"
  prohibitions:
    - "No HowTo schema on comparison pages (D-04, roadmap criterion 2) — remove faqPageGraph, do not add HowTo"
    - "No answer-first verdict behind Reveal/opacity:0 (crawler-visibility invariant)"
    - "No self-made Review/aggregateRating without the reviewsFetchedAt gate (D-04)"
    - "No second TrustBand — mount KeyPageChrome ONCE, never showTrustBand={false}, never in layout (guard 6a4295e)"
    - "Category-alternative pages name NO competitor salons (D-02) — salon-vs-DIY/category only"
---

<objective>
Deliver the comparison set (EXP-01): retrofit the 3 existing comparisons to answer-first (verdict → table → detail), add the 3 category-alternative entries (salon vs DIY, no named rivals), swap the route's schema from FAQ to Product/Review/Breadcrumb, and mount KeyPageChrome once per page. All 6 comparisons get 4-locale content following the EN-first authorship workflow (D-13).

Purpose: EXP-01 is the highest-leverage answer-first surface. The route + ComparisonTable + slug machinery already exist (PATTERNS: "exact" analogs); this is mostly fill + restructure, not greenfield.
Output: comparisons.ts extended to 6 entries, 4-locale dictionary content for all 6, and a retrofitted route emitting Product/Review/Breadcrumb with KeyPageChrome.
</objective>

<execution_context>
@/Users/theduy/Repo/SS-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/theduy/Repo/SS-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-expansion/04-CONTEXT.md
@.planning/phases/04-content-expansion/04-PATTERNS.md
@.planning/phases/04-content-expansion/04-UI-SPEC.md
@.planning/phases/04-content-expansion/04-01-SUMMARY.md

@src/lib/comparisons.ts
@src/app/[lang]/comparisons/[slug]/page.tsx
@src/components/ComparisonTable.tsx
@src/app/[lang]/services/[slug]/page.tsx
@src/components/KeyPageChrome.tsx
@src/lib/consent.server.ts
@src/lib/dictionary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend comparisons.ts with 3 category-alternative entries</name>
  <files>src/lib/comparisons.ts</files>
  <read_first>
    - src/lib/comparisons.ts (full — ComparisonId union, Comparison type, registry shape, all 4 helpers)
    - src/lib/services.ts lines 11 (ServiceId union) + 77-90 (servicePath / servicePathsByLocale convention for localized slugs)
    - .planning/phases/04-content-expansion/04-CONTEXT.md D-02 (the 3 alternatives and their service mappings: manicure, lash-extensions, waxing)
    - .planning/phases/04-content-expansion/04-PATTERNS.md "src/lib/comparisons.ts — extend" section
  </read_first>
  <action>
    Add three ids to the ComparisonId union — for salon-gel-vs-DIY-kit (service manicure), salon-lash-vs-DIY-lash (service lash-extensions), salon-wax-vs-home-wax (service waxing). Append 3 entries to the `comparisons` array using the identical `{ id, slug: {fr,en,es,ar}, service }` shape — localized per-locale slugs, factual and NAP-consistent (D-09). No named competitor in any slug (D-02). Pure data extension — do NOT change the type shape or any of the 4 helper functions; they already iterate the array.
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>comparisons array has 6 entries; ComparisonId union has 6 members; tsc surfaces the (expected) error that en.json lacks the 3 new comparison keys — resolved in Task 2. Helper functions unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Author 4-locale dictionary content for all 6 comparisons (EN-first, D-13)</name>
  <files>src/dictionaries/en.json, src/dictionaries/fr.json, src/dictionaries/es.json, src/dictionaries/ar.json</files>
  <read_first>
    - src/dictionaries/en.json lines 969-1186 (existing `comparisons` block — the canonical key shape Dictionary type derives from)
    - src/lib/dictionary.ts (Dictionary = typeof en — so en.json defines the schema; the 3 new keys MUST exist in en.json or every page typechecks fail)
    - .planning/phases/04-content-expansion/04-CONTEXT.md D-01/D-03 (verdict 40-60 words, honest/neutral stance: DIY cheaper/faster, salon longer-lasting/cleaner/lower-risk, then route to booking)
    - .planning/phases/04-content-expansion/04-PATTERNS.md dictionaries section (comparison key shape: title/metaTitle/metaDescription/intro/columns/rows/verdict/faq)
  </read_first>
  <action>
    EN-FIRST (D-13). Step A: in en.json, add the 3 new category-alternative comparison blocks AND ensure the 3 existing comparisons (gel-vs-regular, lash-styles, wax-vs-sugar) each carry a 40-60 word answer-first `verdict` string with the honest/neutral stance (D-03) — name the real DIY-vs-salon tradeoffs, give a recommendation, then point to booking; no DIY disparagement. Each block matches the existing key shape exactly (title/metaTitle/metaDescription/intro/columns/rows/verdict/faq) so the Dictionary type stays valid. Step B (translation, explicit): mirror all 6 blocks into fr/es/ar with the same keys, translated factually — keys identical across all four files so no type drift. NAP and any service names stay consistent with site.ts. Do NOT author price ranges in comparison copy (comparison verdicts are qualitative; reserve `[PRICE:*]` tokens for the guide cost copy in Plan 03). PAUSE for EN review per D-13 before the fr/es/ar translation step — surface the EN drafts at the checkpoint below.
  </action>
  <verify>
    <automated>bun run test -- src/lib/price-tokens.test.ts && bunx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>All 4 dictionaries carry 6 comparison blocks with identical key sets; tsc passes (no missing-key errors); price-token gate green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Review EN comparison copy before fr/es/ar translation (D-13)</name>
  <files>src/dictionaries/en.json</files>
  <action>HALT after EN authoring (Task 2 Step A), before translation (Step B). Present the EN `comparisons` block for the 6 entries to the user for review. What was built: EN copy for all 6 comparisons (3 retrofit verdicts + 3 new category-alternative blocks) — verdict, intro, table rows, FAQ. How to verify: (1) review the `comparisons` block in src/dictionaries/en.json for the 6 entries; (2) confirm each verdict is 40-60 words, honest/neutral (DIY tradeoffs named, then salon recommendation + booking), no competitor names; (3) confirm factual accuracy of table rows for the 3 new salon-vs-DIY pages. Only after the user approves EN does the agent translate to fr/es/ar (D-13).</action>
  <verify><human-check>User has reviewed the EN comparison copy and replied "approved" (or supplied edits, which are applied before translation).</human-check></verify>
  <done>User approved the EN copy; fr/es/ar translation may proceed. Resume signal: user types "approved" or describes edits.</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit the comparison route — answer-first + Product/Review/Breadcrumb + KeyPageChrome</name>
  <files>src/app/[lang]/comparisons/[slug]/page.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-static-params.md AND node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md (this is a NON-STANDARD Next.js per AGENTS.md — params is a Promise, generateStaticParams receives params as a prop; confirm against the installed docs before editing route signatures)
    - src/app/[lang]/comparisons/[slug]/page.tsx (full — current structure: faqPageGraph+breadcrumbGraph at lines 58-65, intro section 67-87, table+verdict 89-102)
    - src/app/[lang]/services/[slug]/page.tsx lines 7,16-17,56-58 (KeyPageChrome + readConsent + consentKnown mount pattern)
    - src/components/KeyPageChrome.tsx lines 21-29 (props: locale, dict, consentKnown, showTrustBand defaults true)
    - .planning/phases/04-content-expansion/04-01-SUMMARY.md (productGraph/reviewGraph signatures from Plan 01)
    - .planning/phases/04-content-expansion/04-PATTERNS.md "comparisons page — retrofit" + "Answer-first SSR invariant" + "KeyPageChrome mount" sections
  </read_first>
  <action>
    Restructure to verdict → table → detail. Move the 40-60 word verdict to the top intro section as a BARE `<p className="...text-lg leading-relaxed text-mocha">` — NOT wrapped in Reveal (crawler-visibility invariant); back-nav, h1, secondary intro, table, FAQ, CTA may keep Reveal. Swap schema: REMOVE `faqPageGraph` (no HowTo/FAQ rich-result play, D-04); ADD `productGraph(lang, {name: c.title, description: c.metaDescription, path: comparisonPath(cmp, lang)})`, `reviewGraph(lang)` (mounts safely — returns null under the gate), and keep `breadcrumbGraph` with the Home → Services → {title} trail. Import the new builders from @/lib/seo. Add KeyPageChrome: import KeyPageChrome + readConsent, compute `consentKnown` in the server component, mount `<KeyPageChrome locale={lang} dict={dict} consentKnown={consentKnown} />` ONCE near the top of the returned fragment — do NOT pass showTrustBand={false}, do NOT mount in layout (D-11: KeyPageChrome mounted exactly once per page; no duplicate TrustBand/rating bar — guard the 6a4295e double-TrustBand bug). Keep generateStaticParams/generateMetadata as-is.
  </action>
  <verify>
    <automated>bun run test && bunx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Route emits Product+Review+Breadcrumb (no FAQ/HowTo); verdict is a bare SSR <p>; KeyPageChrome mounts once. tsc + full `bun run test` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dictionary copy → SSR HTML/JSON-LD | verdict + table + schema text rendered into the page; schema via JsonLd escape |
| localized slug → comparisonBySlug | wrong-locale slug must 404, not render mismatched content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering (XSS) | comparison route JSON-LD | mitigate | productGraph/reviewGraph render through the existing JsonLd `<` escape path; no new sink |
| T-04-05 | Information disclosure | reviewGraph on comparison page | mitigate | reviewGraph gated on reviewsFetchedAt (Plan 01) — emits nothing under current fixture (D-04) |
| T-04-06 | Repudiation (false claims) | category-alternative copy | accept | Salon-vs-DIY only, no named rivals, honest/neutral stance (D-02/D-03) — no comparative-claim liability |
| T-04-SC | Tampering | npm installs | n/a | No package installs this plan |
</threat_model>

<verification>
- After build, `curl -s http://localhost:3000/en/comparisons/<en-slug> | grep -i "<verdict opening words>"` shows the verdict in raw SSR HTML; repeat for fr/es/ar slugs.
- `curl ... | grep '"@type":"Product"'` and `grep '"@type":"BreadcrumbList"'` present; `grep -c '"@type":"HowTo"'` is 0; `grep -c 'TrustBand'`-equivalent confirms a single trust band mount.
- `bun run test` (incl. price-token gate) and `bunx tsc --noEmit` pass.
</verification>

<success_criteria>
6 comparison pages render answer-first verdict (bare SSR <p>) + scannable table in all 4 locales, emit Product/Review/Breadcrumb (no HowTo), mount KeyPageChrome once, and name no competitors. All tests + typecheck green.
</success_criteria>

<output>
Create `.planning/phases/04-content-expansion/04-02-SUMMARY.md` when done — list the 6 comparison ids + their per-locale slugs (Plan 04 needs them for sitemap/llms.txt).
</output>
