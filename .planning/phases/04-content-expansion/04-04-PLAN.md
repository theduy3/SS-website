---
phase: 04-content-expansion
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - src/app/sitemap.ts
  - src/app/llms.txt/route.ts
  - src/app/[lang]/services/[slug]/page.tsx
autonomous: true
requirements: [EXP-01, EXP-02]
must_haves:
  truths:
    - "sitemap.xml includes all 6 comparison + 3 guide routes across all 4 locales with x-default + accurate lastModified"
    - "llms.txt lists the new comparison + guide pages under their own sections"
    - "Service pages list related comparisons AND related guides (reciprocal linking)"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "guideEntries block + PAGE_DATES for all 9 routes"
      contains: "guideEntries"
    - path: "src/app/llms.txt/route.ts"
      provides: "Comparisons + Guides link sections"
      contains: "guidePath"
    - path: "src/app/[lang]/services/[slug]/page.tsx"
      provides: "reciprocal guidesForService related-reading block"
      contains: "guidesForService"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/guides.ts"
      via: "guideEntries maps guides × locales via guidePath"
      pattern: "guides.*guidePath|guidePath"
    - from: "src/app/[lang]/services/[slug]/page.tsx"
      to: "src/lib/guides.ts"
      via: "guidesForService(service.id) renders related-guides links"
      pattern: "guidesForService"
  prohibitions:
    - "No /guides or /comparisons route added to STANDALONE_PATHS (D-10 — localized routes)"
    - "No hardcoded slug strings in sitemap/llms.txt — import registries and template via guidePath/comparisonPath"
---

<objective>
Wire the 9 new/changed routes into the site's machine surface and internal-link graph: sitemap entries (all 4 locales + x-default + accurate lastModified), llms.txt curated-brief links, and reciprocal "related guides" links on service pages (D-12). This is the cross-cutting wiring that depends on both registries being final.

Purpose: Without sitemap + llms.txt entries the new pages are not advertised to crawlers/AI; without reciprocal links the topical cluster (D-12) is one-directional. All three edits reuse exact existing analogs.
Output: guideEntries in sitemap, Comparisons/Guides sections in llms.txt, related-guides block on service pages.
</objective>

<execution_context>
@/Users/theduy/Repo/SS-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/theduy/Repo/SS-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-expansion/04-CONTEXT.md
@.planning/phases/04-content-expansion/04-PATTERNS.md
@.planning/phases/04-content-expansion/04-02-SUMMARY.md
@.planning/phases/04-content-expansion/04-03-SUMMARY.md

@src/app/sitemap.ts
@src/app/llms.txt/route.ts
@src/app/[lang]/services/[slug]/page.tsx
@src/lib/comparisons.ts
@src/lib/guides.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add guideEntries + PAGE_DATES for all 9 routes to sitemap.ts</name>
  <files>src/app/sitemap.ts</files>
  <read_first>
    - src/app/sitemap.ts (full — PAGE_DATES lines 15-30, FALLBACK_DATE, comparisonEntries block lines 73-88, return array lines 125-131)
    - src/lib/guides.ts (guides, guidePath, guidePathsByLocale)
    - .planning/phases/04-content-expansion/04-02-SUMMARY.md + 04-03-SUMMARY.md (the 6 comparison + 3 guide ids and per-locale slugs for PAGE_DATES keys)
    - .planning/phases/04-content-expansion/04-PATTERNS.md "src/app/sitemap.ts" section (exact guideEntries block to copy)
  </read_first>
  <action>
    Import `guides, guidePath` from @/lib/guides. Add a `guideEntries` block immediately after `comparisonEntries`, copying that block verbatim and substituting guides/guidePath (per-locale alternates + x-default → defaultLocale, changeFrequency monthly, priority 0.6). Add `...guideEntries` to the return array. Add PAGE_DATES entries for the 6 comparison paths + 3 guide paths (use the EN base path as the key, matching how comparison paths are keyed) with accurate lastModified dates so sitemap lastModified is not the fallback. Do NOT hardcode slug strings — template through guidePath/comparisonPath.
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | head -5 && bun run test -- src/app/sitemap 2>&1 | tail -5</automated>
  </verify>
  <done>sitemap return array includes guideEntries; PAGE_DATES covers all 9 routes; tsc passes. A built sitemap.xml lists comparison + guide URLs for all 4 locales with x-default.</done>
</task>

<task type="auto">
  <name>Task 2: Add Comparisons + Guides link sections to llms.txt</name>
  <files>src/app/llms.txt/route.ts</files>
  <read_first>
    - src/app/llms.txt/route.ts lines 56-70 (## Key Pages block + body template + Response)
    - src/lib/comparisons.ts (comparisons, comparisonPath) + src/lib/guides.ts (guides, guidePath)
    - .planning/phases/04-content-expansion/04-PATTERNS.md "src/app/llms.txt/route.ts" section (import + template-map pattern)
  </read_first>
  <action>
    Import `comparisons, comparisonPath` and `guides, guidePath`. Add a `## Comparisons` and a `## Guides` section to the body template after `## Key Pages`, generating EN links by mapping each registry through its path helper — `${comparisons.map((c) => `- ${...}: ${site.url}/en${comparisonPath(c, "en")}`).join("\n")}` and the guides equivalent. Use a short human label per entry (derive from the dictionary title or a static label map — do NOT paste raw ids as the visible label). Do NOT hardcode slug strings.
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>llms.txt body includes Comparisons + Guides sections listing all 9 EN URLs templated from the registries; tsc passes. `curl /llms.txt | grep guides` shows the guide links.</done>
</task>

<task type="auto">
  <name>Task 3: Add reciprocal related-guides links to service pages (D-12)</name>
  <files>src/app/[lang]/services/[slug]/page.tsx</files>
  <read_first>
    - src/app/[lang]/services/[slug]/page.tsx lines 20 (comparisonsForService import), 56 (relatedComparisons), 222-245 (the related-comparisons render block to mirror)
    - src/lib/guides.ts (guidesForService, guidePath)
    - .planning/phases/04-content-expansion/04-PATTERNS.md "Service pages — reciprocal guides links" section
  </read_first>
  <action>
    Import `guidesForService, guidePath` from @/lib/guides alongside the existing comparisons import. Compute `const relatedGuides = guidesForService(service.id);`. Render a "Related Guides" list mirroring the existing relatedComparisons block exactly — same wrapper, same `<Link href={`/${lang}${guidePath(g, lang)}`}>` mapping, guarded by `relatedGuides.length > 0`. Reuse the existing related-reading UI; add NO new component. Use a localized label from the dictionary (reuse the comparison-related label key or add a parallel `guides`-related label if one is needed — keep it consistent with the existing block).
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | head -5 && bun run test 2>&1 | tail -5</automated>
  </verify>
  <done>Service pages render a related-guides list when guidesForService returns entries, mirroring related-comparisons; tsc + full `bun run test` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry data → sitemap/llms.txt/service-page links | route URLs templated from registries into crawler-facing surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering (broken links) | sitemap/llms.txt URLs | mitigate | URLs templated through guidePath/comparisonPath (single source) — no hand-typed slugs that can drift from the registry |
| T-04-11 | Information disclosure | llms.txt curated brief | accept | Only public marketing pages listed; no internal/admin routes |
| T-04-SC | Tampering | npm installs | n/a | No package installs this plan |
</threat_model>

<verification>
- `bunx tsc --noEmit` + `bun run test` pass (price gate is green only after the user fills tokens — note this in the SUMMARY).
- Built `sitemap.xml` contains all 6 comparison + 3 guide URLs × 4 locales with x-default and non-fallback lastModified.
- `curl /llms.txt | grep -E "comparisons|guides"` lists the new pages.
- A service page (e.g. /en/services/manicure) renders both related comparisons and related guides.
- `grep -i "guides\|comparisons" src/proxy.ts` confirms neither route is in STANDALONE_PATHS (D-10).
</verification>

<success_criteria>
All 9 routes are in sitemap (4 locales + x-default + accurate lastModified) and llms.txt; service pages link reciprocally to related comparisons and guides; no localized route leaked into STANDALONE_PATHS. Tests + typecheck green.
</success_criteria>

<output>
Create `.planning/phases/04-content-expansion/04-04-SUMMARY.md` when done — confirm the 9 routes wired into sitemap/llms.txt + reciprocal links, and flag whether [PRICE:*] tokens remain unfilled (build gate status).
</output>
