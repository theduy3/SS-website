---
phase: 03-measurement-conversion
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/Stars.tsx
  - src/components/TrustBand.tsx
  - src/components/StickyCtaBar.tsx
  - src/components/KeyPageChrome.tsx
  - src/components/ContactForm.tsx
  - src/app/[lang]/page.tsx
  - src/app/[lang]/services/page.tsx
  - src/app/[lang]/services/[slug]/page.tsx
  - src/app/[lang]/faq/page.tsx
  - src/app/[lang]/laval/page.tsx
  - src/dictionaries/en.json
  - src/dictionaries/fr.json
  - src/dictionaries/es.json
  - src/dictionaries/ar.json
  - src/components/TrustBand.test.tsx
autonomous: true
requirements: [MEAS-02, MEAS-03]

must_haves:
  truths:
    - "Each key page (home, services index, /services/[slug], faq, /laval) shows an above-fold trust band in SSR HTML with the rating, review count, and a static 'Since 2024' marker"
    - "Trust band shows the rating block only when live Google review data is present (reviewsFetchedAt gate); otherwise shows 'Since 2024' only"
    - "On mobile, key pages show a fixed-bottom Call+Book sticky bar (md:hidden); it is suppressed until a consent decision is recorded"
    - "Clicking the sticky Call button fires a phone_click GA4 event and still initiates the tel: call"
    - "Clicking the sticky Book button fires a book_cta_click GA4 event and navigates to /{locale}/appointments"
    - "Submitting the contact form successfully fires a generate_lead event with method:contact_form"
    - "The trust band and sticky bar render on the 5 key pages and do NOT render on contact/appointments/legal pages"
  artifacts:
    - path: "src/components/TrustBand.tsx"
      provides: "SSR server component: live-gated rating + reviewCount + static Since 2024"
    - path: "src/components/StickyCtaBar.tsx"
      provides: "Client md:hidden fixed-bottom Call+Book bar firing phone_click/book_cta_click"
    - path: "src/components/KeyPageChrome.tsx"
      provides: "Wrapper that renders TrustBand + StickyCtaBar; one import per key page"
    - path: "src/components/Stars.tsx"
      provides: "size prop applied to the <svg> (enables h-4 w-4 trust-band star)"
    - path: "src/components/ContactForm.tsx"
      contains: "track(\"generate_lead\""
  key_links:
    - from: "src/components/StickyCtaBar.tsx"
      to: "src/lib/analytics.ts"
      via: "track('phone_click') / track('book_cta_click') in click wrappers"
      pattern: "track\\(\"(phone_click|book_cta_click)\""
    - from: "src/components/ContactForm.tsx"
      to: "src/lib/analytics.ts"
      via: "track('generate_lead') in success branch"
      pattern: "track\\(\"generate_lead\""
    - from: "src/app/[lang]/page.tsx"
      to: "src/components/KeyPageChrome.tsx"
      via: "KeyPageChrome mounted on home"
      pattern: "KeyPageChrome"
    - from: "src/components/TrustBand.tsx"
      to: "src/lib/reviews.ts"
      via: "reviewsFetchedAt live-data gate"
      pattern: "reviewsFetchedAt"
  prohibitions:
    - "Button.tsx MUST NOT receive an onClick prop — events fire via <span onClick> wrappers around the Button (UI-SPEC line 174, D-06)"
    - "TrustBand MUST NOT be wrapped in <Reveal> or any Framer Motion / opacity:0 (hides it from crawlers — Pitfall 3)"
    - "TrustBand MUST NOT use new Date() — render the static 'Since 2024' dictionary string only (D-08)"
    - "TrustBand / StickyCtaBar MUST NOT be mounted in [lang]/layout.tsx (would leak onto non-key pages — D-09 / Pitfall 5)"
    - "TrustBand MUST NOT render a placeholder rating when live data is absent (show 'Since 2024' only)"
---

<objective>
Deliver the conversion + trust vertical slice: above-fold trust band (rating + review count, live-gated, + static "Since 2024") and a mobile sticky Call+Book bar on exactly the 5 key pages, plus the three conversion events (`phone_click`, `book_cta_click`, `generate_lead`) firing through the guarded `track()` helper from plan 03-01. This is the slice "a mobile visitor sees credibility above the fold, can tap Call or Book from a persistent bar, and those taps (plus contact-form success) reach GA4" — MEAS-02 + MEAS-03.

Purpose: trust signals lift conversion and must be crawler-visible (SSR); the sticky bar (D-07: sticky-bottom mobile-only Call+Book, z-40 below modals) makes Call/Book reachable in one tap on mobile; the events make conversion measurable. `book_cta_click` is click-intent only — booking completes in the cross-domain SalonX widget and is unmeasured (D-05). There is no shared key-page layout segment, so the chrome mounts per-page via one `<KeyPageChrome>` import (D-09 / RESEARCH Pitfall 5).
Output: `Stars` size prop, `TrustBand.tsx`, `StickyCtaBar.tsx`, `KeyPageChrome.tsx`, `ContactForm` generate_lead hook, trust dictionary keys in 4 locales, and mounts on the 5 key pages.
</objective>

<execution_context>
@/Users/theduy/Repo/SS-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/theduy/Repo/SS-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-measurement-conversion/03-CONTEXT.md
@.planning/phases/03-measurement-conversion/03-RESEARCH.md
@.planning/phases/03-measurement-conversion/03-PATTERNS.md
@.planning/phases/03-measurement-conversion/03-UI-SPEC.md
@.planning/phases/03-measurement-conversion/03-01-PLAN.md
</context>

<artifacts_produced>
This plan creates / extends:
- `src/components/Stars.tsx` — adds a `size` prop (default `"h-6 w-6"`) applied to the `<svg>` so callers can pass `"h-4 w-4"`.
- `src/components/TrustBand.tsx` — `<TrustBand locale dict reviews />` server component (rating live-gated + static "Since 2024").
- `src/components/StickyCtaBar.tsx` — `<StickyCtaBar locale dict consentKnown />` client component (Call+Book, fires phone_click/book_cta_click).
- `src/components/KeyPageChrome.tsx` — `<KeyPageChrome locale dict consentKnown />` (renders TrustBand + StickyCtaBar; one import per key page).
- `src/components/ContactForm.tsx` — `track("generate_lead", {method:"contact_form"})` added in the success branch (after line 34).
- `src/dictionaries/{en,fr,es,ar}.json` — new `trust` namespace (`established`, `reviewsWord`); verifies `cta.callNow`/`cta.book` exist in all 4.
- Mounts on `src/app/[lang]/{page,services/page,services/[slug]/page,faq/page,laval/page}.tsx` (KeyPageChrome import + `pb-[64px] md:pb-0` body padding).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Stars size prop + TrustBand server component + trust dictionary keys</name>
  <files>src/components/Stars.tsx, src/components/TrustBand.tsx, src/components/TrustBand.test.tsx, src/dictionaries/en.json, src/dictionaries/fr.json, src/dictionaries/es.json, src/dictionaries/ar.json</files>
  <read_first>
    - src/components/Stars.tsx (file being modified — the <svg> hardcodes `h-6 w-6` at line 7; className lands on the wrapping div at line 5, so a real size prop on the svg is required — PATTERNS line 132)
    - src/components/TrustBand.tsx (new — does not exist)
    - src/components/ReviewCard.tsx (analog: Stars + rating block, server component, NO "use client" — PATTERNS line 124-130)
    - src/app/[lang]/page.tsx (analog: live site.reviews rating formatting at lines 34-39 — localeTag/toLocaleString; currently only en/fr branch, must generalize for es/ar — PATTERNS line 146-152)
    - src/lib/seo.ts (analog: the reviewsFetchedAt live-data gate, lines 163-169 — PATTERNS line 134-143)
    - src/lib/reviews.ts (provides reviewsFetchedAt)
    - src/lib/site.ts (site.reviews.{ratingValue,reviewCount,bestRating}; site.established added in 03-01)
    - src/dictionaries/en.json (files being modified — trust namespace placement)
    - .planning/phases/03-measurement-conversion/03-UI-SPEC.md Surface 3 (lines 225-256), Copywriting Contract lines 274-281
  </read_first>
  <action>
    Modify `src/components/Stars.tsx`: add a `size` prop (default `"h-6 w-6"` to preserve every existing call site) and apply it to the `<svg>` className (replace the hardcoded `h-6 w-6` on the svg at line 7). Keep `className` on the wrapping div for color/layout. This is the surgical fix from PATTERNS line 132 so the trust band can pass `size="h-4 w-4"`.
    Create `src/components/TrustBand.tsx` as a SERVER component (NO `"use client"`, mirror `ReviewCard.tsx`). Signature `TrustBand({ locale, dict, reviews }: { locale: Locale; dict: Pick<Dictionary,"trust">; reviews: typeof site.reviews })` (or read `site.reviews` directly — Claude's discretion). Band shell `bg-fog py-2 px-6` with `flex flex-wrap items-center gap-3 max-w-7xl mx-auto` inner (UI-SPEC lines 238-249). Gate the rating block on the `reviewsFetchedAt` truthiness (import from `@/lib/reviews`, mirror seo.ts:163-169): when truthy render `<Stars className="text-espresso" size="h-4 w-4" />`, the rating number (`text-lg font-semibold text-espresso`), an `aria-hidden` `·` separator (`text-tan`), `reviewCount` + `dict.trust.reviewsWord` (`text-sm text-mocha`), another separator; ALWAYS render `dict.trust.established` (`text-sm text-tan`, muted). When `reviewsFetchedAt` is falsy, render `dict.trust.established` only (no placeholder rating — UI-SPEC line 233). Format the numbers with a locale-aware tag generalized for all 4 locales (extend the home page `localeTag` from `lang==="fr"?"fr-CA":"en-CA"` to map `es`→`es`, `ar`→`ar-CA` or similar; `toLocaleString` with `minimumFractionDigits:1` for the rating). NO `new Date()`. NOT wrapped in `<Reveal>`.
    Create `src/components/TrustBand.test.tsx`: assert (a) when reviewsFetchedAt truthy, output contains the rating number + reviewsWord + established; (b) when falsy, output contains established and NOT the rating block. Follow the existing page.test.tsx / *.test.tsx SSR-render style.
    Add a `trust` namespace (`established`, `reviewsWord`) to all 4 dictionaries with the EXACT copy from UI-SPEC lines 276-279 (en/fr/es/ar). Add to en.json first (type source), then mirror. Also verify `cta.callNow` and `cta.book` exist in all 4 locales; add any missing from UI-SPEC line 281 / 290.
  </action>
  <verify>
    <automated>bun run test src/components/TrustBand.test.tsx 2>&1 | tail -20; for f in en fr es ar; do node -e "const d=require('./src/dictionaries/$f.json'); if(!d.trust||!d.trust.established||!d.trust.reviewsWord||!d.cta||!d.cta.callNow||!d.cta.book){process.exit(1)}" && echo "$f trust+cta ok" || echo "$f MISSING keys"; done; grep -c 'size' src/components/Stars.tsx; bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Stars.tsx` accepts a `size` prop and applies it to the `<svg>` (grep: `size` present; the svg no longer hardcodes `h-6 w-6` as the only size source).
    - `TrustBand.tsx` has NO `"use client"` directive and is NOT wrapped in `<Reveal>` (grep `Reveal` returns 0 in TrustBand.tsx).
    - `TrustBand.tsx` gates the rating on `reviewsFetchedAt` (grep >=1) and contains no `new Date(` (grep returns 0).
    - `bun run test src/components/TrustBand.test.tsx` passes both gate cases.
    - All 4 dictionaries expose `trust.established`, `trust.reviewsWord`, `cta.callNow`, `cta.book` (node check prints "ok" ×4).
    - `bun run tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Stars takes a size prop; TrustBand is SSR-only, live-gated, static "Since 2024", no motion/no Date(); trust+cta copy present in 4 locales; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: StickyCtaBar (click→track wrappers) + KeyPageChrome + ContactForm generate_lead</name>
  <files>src/components/StickyCtaBar.tsx, src/components/KeyPageChrome.tsx, src/components/ContactForm.tsx</files>
  <read_first>
    - src/components/StickyCtaBar.tsx (new — does not exist)
    - src/components/Header.tsx (analog: sticky bar + Button + localizedHref + md:hidden + RTL — cite lines 32-33, 36-37, 82-84; PATTERNS line 76-118)
    - src/components/Button.tsx (reference ONLY — renders <a>/<Link> at lines 38-55; do NOT add onClick; variant="light" at line 8)
    - src/lib/analytics.ts (track — from 03-01)
    - src/lib/consent.ts (readConsent / client cookie presence — from 03-01)
    - src/lib/site.ts (site.contact.phoneHref = "tel:+14505056450"; site.booking = "/appointments" — NOTE phoneHref is site.contact.phoneHref NOT site.phoneHref)
    - src/components/ContactForm.tsx (file being modified — success branch: setStatus("success") at line 34, form.reset() at line 35; already "use client")
    - src/components/KeyPageChrome.tsx (new — does not exist)
    - .planning/phases/03-measurement-conversion/03-UI-SPEC.md Surface 2 (lines 188-221), Interaction Contract lines 327-334, RTL Contract lines 372-380
    - .planning/phases/03-measurement-conversion/03-RESEARCH.md §4 (event wrapper that does NOT touch Button.tsx), §5 (generate_lead)
  </read_first>
  <action>
    Create `src/components/StickyCtaBar.tsx` as `"use client"` `StickyCtaBar({ locale, dict, consentKnown }: { locale: Locale; dict: Pick<Dictionary,"cta">; consentKnown: boolean })`. Suppress render while consent undecided (return null when `consentKnown === false`, OR check the client cookie presence) to avoid a double bottom-bar with ConsentBar (UI-SPEC line 200, RESEARCH Open Q4). Bar shell `fixed bottom-0 left-0 right-0 z-40 bg-espresso text-cream py-3 px-4 md:hidden` with inner `flex gap-3 items-center justify-center max-w-7xl mx-auto` (UI-SPEC lines 209-210); `z-40` keeps it below Header `z-50` and PopupHost `z-[100]`. Copy `localizedHref` verbatim from `Header.tsx:32-33`. Render the two CTAs as Buttons wrapped in `<span onClick>` interceptors (RESEARCH §4 / PATTERNS line 101-114) — DO NOT add onClick to Button.tsx: Call = `<span onClick={() => track("phone_click")}><Button href={site.contact.phoneHref} variant="light" className="flex-1 min-h-[44px]">{dict.cta.callNow}</Button></span>`; Book = `<span onClick={() => track("book_cta_click")}><Button href={localizedHref(site.booking)} variant="light" className="flex-1 min-h-[44px]">{dict.cta.book}</Button></span>`. The inner `<a>`/`<Link>` still performs the tel:/navigation. RTL: apply `flex-row-reverse` when `dirFor(locale)==="rtl"` so order reads [Book][Call] (UI-SPEC line 378). No animation.
    Create `src/components/KeyPageChrome.tsx` (server component) `KeyPageChrome({ locale, dict, consentKnown }: ...)` that renders `<TrustBand .../>` then `<StickyCtaBar .../>`. This is the single "key page = one import" mount (RESEARCH structure note line 200). Pass the narrowed dict picks each child needs.
    Modify `src/components/ContactForm.tsx`: add `import { track } from "@/lib/analytics";` to the import block (lines 1-4) and insert `track("generate_lead", { method: "contact_form" });` immediately after `setStatus("success")` (line 34, before `form.reset()` at line 35). Send ONLY `method` — no form field values (PII rule, RESEARCH Security V7).
  </action>
  <verify>
    <automated>grep -c 'track("phone_click")\|track("book_cta_click")' src/components/StickyCtaBar.tsx; grep -c 'track("generate_lead"' src/components/ContactForm.tsx; grep -c 'onClick' src/components/Button.tsx; grep -c 'md:hidden' src/components/StickyCtaBar.tsx; grep -c 'TrustBand\|StickyCtaBar' src/components/KeyPageChrome.tsx; bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `StickyCtaBar.tsx` fires `track("phone_click")` and `track("book_cta_click")` via `<span onClick>` wrappers (grep >=2 combined) and is `md:hidden` `fixed bottom-0` `z-40`.
    - `Button.tsx` still contains NO `onClick` prop (grep `onClick` in Button.tsx returns 0 — the ban holds).
    - `ContactForm.tsx` fires `track("generate_lead", { method: "contact_form" })` in the success branch (grep >=1) and sends no form field values.
    - `KeyPageChrome.tsx` renders both `TrustBand` and `StickyCtaBar` (grep >=2).
    - `StickyCtaBar` uses `site.contact.phoneHref` (NOT `site.phoneHref`) and `localizedHref(site.booking)`.
    - `bun run tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>StickyCtaBar fires the two click events via wrappers (Button untouched), suppresses until consent decided; KeyPageChrome bundles both surfaces; ContactForm fires generate_lead on success.</done>
</task>

<task type="auto">
  <name>Task 3: Mount KeyPageChrome on the 5 key pages with body padding</name>
  <files>src/app/[lang]/page.tsx, src/app/[lang]/services/page.tsx, src/app/[lang]/services/[slug]/page.tsx, src/app/[lang]/faq/page.tsx, src/app/[lang]/laval/page.tsx</files>
  <read_first>
    - src/app/[lang]/page.tsx (file being modified — home: has lang/dict in scope at lines 28-32; analog mount pattern for the other 4)
    - src/app/[lang]/services/page.tsx (file being modified)
    - src/app/[lang]/services/[slug]/page.tsx (file being modified)
    - src/app/[lang]/faq/page.tsx (file being modified)
    - src/app/[lang]/laval/page.tsx (file being modified)
    - src/components/KeyPageChrome.tsx (created in Task 2)
    - src/lib/consent.ts (readConsent — from 03-01; pages SSR-read to derive consentKnown)
    - .planning/phases/03-measurement-conversion/03-UI-SPEC.md line 219 (body padding), Surface 3 placement line 229 (trust band below hero, above first content section)
    - .planning/phases/03-measurement-conversion/03-PATTERNS.md "Key pages" section lines 265-273
  </read_first>
  <action>
    On each of the 5 key pages: import `KeyPageChrome` and `readConsent`. SSR-read `const consent = await readConsent()` → `consentKnown = consent !== undefined`. Mount `<KeyPageChrome locale={lang} dict={dict} consentKnown={consentKnown} />` — render TrustBand placement BELOW the hero/PageHeader and above the first content section (UI-SPEC line 229); StickyCtaBar is fixed so its position in the tree does not matter, but KeyPageChrome renders both, so place the chrome where the trust band belongs visually (Claude's discretion if TrustBand and StickyCtaBar need separate mount points — acceptable to split KeyPageChrome into the two surfaces at the page level if cleaner). Add `pb-[64px] md:pb-0` to each page's outermost wrapper / `<main>`-equivalent so the fixed sticky bar does not cover bottom content (UI-SPEC line 219). Do this on home, services index, services/[slug], faq, laval ONLY. Do NOT add to contact/appointments/legal pages (D-09).
  </action>
  <verify>
    <automated>for p in 'page.tsx' 'services/page.tsx' 'services/[slug]/page.tsx' 'faq/page.tsx' 'laval/page.tsx'; do c=$(grep -c 'KeyPageChrome' "src/app/[lang]/$p"); echo "$p KeyPageChrome=$c"; done; echo "--- must be 0 on non-key pages ---"; grep -rc 'KeyPageChrome' 'src/app/[lang]/contact/page.tsx' 'src/app/[lang]/appointments/page.tsx' 2>/dev/null; bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 5 key pages import and mount `KeyPageChrome` (grep =1 each).
    - `contact` and `appointments` pages do NOT contain `KeyPageChrome` (grep =0).
    - Each key page adds `pb-[64px] md:pb-0` to its wrapper (grep >=1 per page).
    - Runtime (verification phase): `curl .../en/ | grep -i "since 2024"` returns text before `</body>`; `curl .../en/contact | grep -i "callNow\|Call now"` does NOT match the sticky bar.
    - `bun run tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Trust band + sticky bar render on exactly the 5 key pages with correct body padding; absent on non-key pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GA4 | Conversion event payloads (phone_click/book_cta_click/generate_lead) leave the browser |
| user click → tel:/navigation | Click wrappers must not break the underlying navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Info Disclosure | `generate_lead` event | mitigate | Send only `{method:"contact_form"}`; never name/email/message field values (enforced in ContactForm call site) |
| T-03-06 | Tampering | click wrapper around Button | accept | `<span onClick>` is decorative; the inner `<a>` carries href semantics — navigation proceeds even if track() throws (it cannot — guarded) |
| T-03-07 | Privacy (Law 25) | event firing pre-consent | mitigate | All events route through guarded `track()`; with consent denied gtag withholds storage; sticky bar suppressed until a consent decision is recorded |
| T-03-SC | Tampering | npm installs | mitigate | This plan installs NO packages |
</threat_model>

<verification>
- `bun run tsc --noEmit` exits 0.
- TrustBand + ConsentBar tests pass.
- TrustBand present in SSR HTML, not behind opacity/motion (curl grep at verification phase).
- The three events fire (GA4 Realtime → Events at verification phase, with a GA id set and consent granted).
- Sticky bar on 5 key pages only; absent on contact/appointments/legal.
- Button.tsx onClick ban still holds (grep = 0).
</verification>

<success_criteria>
Maps to ROADMAP Phase 3 success criteria 2 (MEAS-02: conversion events for phone/contact-form/booking) and 3 (MEAS-03: sticky mobile CTA + above-fold trust signals on key pages, visible at 375px without scrolling). Trust band is crawler-visible SSR; events fire through the guarded helper; chrome is key-pages-only.
</success_criteria>

<output>
Create `.planning/phases/03-measurement-conversion/03-02-SUMMARY.md` when done.
</output>
