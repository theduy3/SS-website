---
phase: 03-measurement-conversion
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - package.json
  - bun.lock
  - src/components/WebVitalsReporter.tsx
  - src/app/[lang]/layout.tsx
  - next.config.ts
autonomous: false
requirements: [MEAS-04]
user_setup:
  - service: dokploy
    why: "NEXT_PUBLIC_GA_ID must be present at BUILD time for web_vitals events to reach GA4 from statically generated key pages (RESEARCH Open Q2)."
    dashboard_config:
      - task: "Confirm NEXT_PUBLIC_GA_ID is injected into the Docker BUILD stage (not only runtime) — NEXT_PUBLIC_ vars inline at build for static pages"
        location: "Dokploy -> app -> Environment (build args)"

must_haves:
  truths:
    - "After consent is granted, LCP, INP, and CLS metrics are sent to GA4 as web_vitals events with metric name, value, and rating"
    - "Before consent is granted, no web_vitals events are sent (consent-gated)"
    - "ANALYZE=true bun run build produces a client/server bundle treemap"
    - "The bundle analyzer report shows whether framer-motion is loaded once or duplicated across server/client bundles"
    - "When ANALYZE is unset, the normal build is unaffected (analyzer disabled)"
  artifacts:
    - path: "src/components/WebVitalsReporter.tsx"
      provides: "Client component: web-vitals onLCP/onINP/onCLS → guarded track('web_vitals'), consent-gated"
    - path: "next.config.ts"
      contains: "withBundleAnalyzer"
    - path: "package.json"
      contains: "web-vitals"
  key_links:
    - from: "src/components/WebVitalsReporter.tsx"
      to: "src/lib/analytics.ts"
      via: "track('web_vitals', ...) from onLCP/onINP/onCLS callbacks"
      pattern: "track\\(\"web_vitals\""
    - from: "src/app/[lang]/layout.tsx"
      to: "src/components/WebVitalsReporter.tsx"
      via: "WebVitalsReporter mounted with consentGranted prop"
      pattern: "WebVitalsReporter"
    - from: "next.config.ts"
      to: "@next/bundle-analyzer"
      via: "withBundleAnalyzer wraps nextConfig export"
      pattern: "withBundleAnalyzer\\(nextConfig\\)"
  prohibitions:
    - "MUST NOT use onFID (removed in web-vitals v5 — INP is the metric; RESEARCH State of the Art)"
    - "MUST NOT send web_vitals events before consent is granted (Pitfall 6, Law 25)"
    - "MUST NOT speculatively refactor framer-motion import boundaries — refactor ONLY if the analyzer shows duplication (D-11, Rule 3)"
    - "MUST NOT add new framer-motion usages this phase"
---

<objective>
Deliver the RUM + bundle-evidence vertical slice: install `web-vitals` (5.x) and `@next/bundle-analyzer` (16.x), pipe LCP/INP/CLS into GA4 as consent-gated `web_vitals` events via a `WebVitalsReporter` mounted in `[lang]/layout.tsx`, and wire `ANALYZE=true` bundle analysis to produce the server/client treemap that proves whether Framer Motion is double-loaded. This is the slice "real-user CWV metrics reach GA4 after consent, and we have the evidence to confirm no Framer double-load" — MEAS-04.

Purpose: MEAS-04 requires RUM (INP/CLS) captured and key pages meeting "good" CWV with no regression from the added schema/content. D-10: send LCP/INP/CLS as consent-gated GA4 `web_vitals` events (no new infra/endpoint). D-12: the "good" CWV target scope is the same key-page set as D-09. D-11 mandates evidence-first Framer-double-load detection (refactor only if duplication is shown).
Output: `WebVitalsReporter.tsx`, the layout mount, `web-vitals` + `@next/bundle-analyzer` installed, `next.config.ts` wrapped, and an `analyze` script.
</objective>

<execution_context>
@/Users/theduy/Repo/SS-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/theduy/Repo/SS-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-measurement-conversion/03-CONTEXT.md
@.planning/phases/03-measurement-conversion/03-RESEARCH.md
@.planning/phases/03-measurement-conversion/03-PATTERNS.md
@.planning/phases/03-measurement-conversion/03-UI-SPEC.md
@.planning/phases/03-measurement-conversion/03-01-PLAN.md
</context>

<artifacts_produced>
This plan creates / extends:
- `src/components/WebVitalsReporter.tsx` — `<WebVitalsReporter consentGranted />` client component (onLCP/onINP/onCLS → track).
- `next.config.ts` — `withBundleAnalyzer(nextConfig)` wrap gated on `ANALYZE === "true"`.
- `package.json` — `web-vitals` dependency, `@next/bundle-analyzer` devDependency, `analyze` script (`ANALYZE=true next build`).
- `src/app/[lang]/layout.tsx` — `<WebVitalsReporter consentGranted={...} />` mount (consentGranted derived from the SSR cookie read added in 03-01).
- The `ANALYZE` env wiring (build-time, disabled when unset).
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package legitimacy gate (web-vitals, @next/bundle-analyzer)</name>
  <action>STOP. Do not run any install command. Wait for explicit human approval after the verification steps below. This checkpoint is blocking-human and NOT auto-approvable (T-03-SC) — both packages are [SUS] too-new and require a human confirm before install.</action>
  <what-built>Nothing yet — this is a pre-install legitimacy gate. The next tasks install `web-vitals@^5.3.0` and `@next/bundle-analyzer@^16.2.9`. Both were flagged SUS by the package-legitimacy audit ONLY on the `too-new` heuristic (high-frequency first-party republishes); RESEARCH judged both unambiguously legitimate (official GoogleChrome / vercel-next.js repos, millions of weekly downloads, no postinstall scripts).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/web-vitals — confirm publisher is the GoogleChrome org and weekly downloads are in the millions.
    2. Open https://www.npmjs.com/package/@next/bundle-analyzer — confirm it is part of vercel/next.js and tracks the Next 16.2.x line.
    3. Confirm neither has a postinstall script (RESEARCH verified scripts.postinstall = null on both).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the installs, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install web-vitals + @next/bundle-analyzer; wrap next.config.ts; add analyze script</name>
  <files>package.json, bun.lock, next.config.ts</files>
  <read_first>
    - package.json (file being modified — dependencies/devDependencies/scripts; lockfile is bun.lock so use bun)
    - next.config.ts (file being modified — `nextConfig` exported at line 51 via `export default nextConfig`; Turbopack root pinned at line 33)
    - .planning/phases/03-measurement-conversion/03-RESEARCH.md §7, Pattern 5 (analyzer wrap + Turbopack caveat Open Q3/A6), Installation block lines 105-110
    - .planning/phases/03-measurement-conversion/03-PATTERNS.md next.config.ts section lines 277-285
  </read_first>
  <action>
    Install with bun (lockfile is `bun.lock` — global CLAUDE.md package-manager rule): `bun add web-vitals` and `bun add -d @next/bundle-analyzer`. Target versions: `web-vitals@^5.3.0`, `@next/bundle-analyzer@^16.2.9` (RESEARCH verified latest).
    Modify `next.config.ts`: add `import bundleAnalyzer from "@next/bundle-analyzer";` and `const withBundleAnalyzer = bundleAnalyzer({ enabled: process.env.ANALYZE === "true" });`, then change the final export from `export default nextConfig;` (line 51) to `export default withBundleAnalyzer(nextConfig);`. Do NOT alter the existing `securityHeaders`, `output: "standalone"`, `turbopack.root`, or `images` config.
    Add an `"analyze": "ANALYZE=true next build"` script to `package.json`.
    Verify the analyzer emits a treemap under THIS Turbopack build (RESEARCH Open Q3/A6): run `ANALYZE=true bun run build` and confirm a report is produced. If the Turbopack build does not emit the webpack-stats treemap, fall back to running the analyze pass with the webpack builder (omit `--turbopack`) and record that finding in the SUMMARY (it affects how success-criterion 4 is demonstrated). `.next/` is already gitignored so the report output is covered.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['web-vitals']){process.exit(1)}; if(!(p.devDependencies&&p.devDependencies['@next/bundle-analyzer'])){process.exit(1)}; if(!p.scripts.analyze){process.exit(1)}; console.log('deps+script ok')"; grep -c 'withBundleAnalyzer(nextConfig)' next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `web-vitals` in dependencies and `@next/bundle-analyzer` in devDependencies, plus an `analyze` script (node check prints "deps+script ok").
    - `next.config.ts` ends with `export default withBundleAnalyzer(nextConfig);` (grep =1) and preserves the existing securityHeaders/output/turbopack/images config.
    - `ANALYZE=true bun run build` exits 0 and emits a treemap report (or the SUMMARY records the webpack-fallback finding if Turbopack does not).
    - The Framer-Motion duplication finding (single shared chunk vs duplicated) is recorded in the SUMMARY (D-11 evidence).
  </acceptance_criteria>
  <done>Both packages installed via bun; analyzer wraps the config gated on ANALYZE; analyze build produces the treemap; Framer double-load finding recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebVitalsReporter (consent-gated onLCP/onINP/onCLS → track) + layout mount</name>
  <files>src/components/WebVitalsReporter.tsx, src/components/WebVitalsReporter.test.tsx, src/app/[lang]/layout.tsx</files>
  <read_first>
    - src/components/WebVitalsReporter.tsx (new — does not exist)
    - src/components/Reveal.tsx (analog: "use client" + useEffect + returns null shell — PATTERNS line 176-177)
    - src/lib/analytics.ts (track — from 03-01; "web_vitals" is in the GaEvent union)
    - src/lib/consent.ts (readConsent — from 03-01; layout derives consentGranted)
    - src/app/[lang]/layout.tsx (file being modified — mount alongside Header/Footer/PopupHost/ConsentBar; the SSR consent read added in 03-01 already supplies consent state; derive consentGranted = consent === "granted")
    - .planning/phases/03-measurement-conversion/03-RESEARCH.md §3 (verified web-vitals v5 API), Pitfall 6, Pitfall 7
    - node_modules/web-vitals (confirm onLCP/onINP/onCLS exports after install; do NOT use onFID)
  </read_first>
  <behavior>
    - consentGranted=false → no web-vitals listeners registered, no track() calls
    - consentGranted=true → onLCP/onINP/onCLS registered; each callback calls track("web_vitals", {metric_name, value, metric_rating, metric_id})
    - Component renders null (no DOM)
  </behavior>
  <action>
    Create `src/components/WebVitalsReporter.tsx` as `"use client"` `WebVitalsReporter({ consentGranted }: { consentGranted: boolean })`. In a `useEffect` keyed on `consentGranted`: `if (!consentGranted) return;` then `import { onLCP, onINP, onCLS, type Metric } from "web-vitals"` and register all three with a `send` callback that calls `track("web_vitals", { metric_name: m.name, value: Math.round(m.value), metric_rating: m.rating, metric_id: m.id })` (RESEARCH §3; param shapes Claude's discretion D-04). Use LCP/INP/CLS ONLY — `onFID` does not exist in v5. `return null`. Consent-gated registration satisfies Pitfall 6 (no events pre-consent); web-vitals delivers final values per Pitfall 7 (use defaults, no custom observer).
    Create `src/components/WebVitalsReporter.test.tsx`: mock `web-vitals` and `track`; assert no registration / no track when consentGranted=false, and registration + track on metric callback when true. Follow existing *.test.tsx style.
    Modify `src/app/[lang]/layout.tsx`: derive `const consentGranted = consent === "granted"` from the SSR consent read already added in 03-01, and mount `<WebVitalsReporter consentGranted={consentGranted} />` alongside the existing mounts (Header/Footer/PopupHost/ConsentBar/Analytics). NOTE: 03-01 and 03-03 both edit layout.tsx — they run in different waves (03-01 wave 1, 03-03 wave 2) so the 03-01 cookie-read scaffold is present before this edit.
  </action>
  <verify>
    <automated>bun run test src/components/WebVitalsReporter.test.tsx 2>&1 | tail -20; grep -c 'track("web_vitals"' src/components/WebVitalsReporter.tsx; grep -c 'onFID' src/components/WebVitalsReporter.tsx; grep -c 'WebVitalsReporter' 'src/app/[lang]/layout.tsx'; bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/components/WebVitalsReporter.test.tsx` passes both consent-gate cases.
    - `WebVitalsReporter.tsx` fires `track("web_vitals", ...)` (grep >=1) and contains NO `onFID` (grep =0).
    - `WebVitalsReporter.tsx` registers listeners only when `consentGranted` is true (gate in useEffect).
    - `[lang]/layout.tsx` mounts `<WebVitalsReporter` with a `consentGranted` prop (grep >=1).
    - `bun run tsc --noEmit` exits 0.
    - Runtime (verification phase, GA id set + consent granted): GA4 Realtime → Events shows `web_vitals` with LCP/INP/CLS; none appear before Accept.
  </acceptance_criteria>
  <done>WebVitalsReporter sends LCP/INP/CLS to GA4 only after consent; uses v5 API (no onFID); mounted in layout with the SSR-derived consentGranted prop; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GA4 | web_vitals metric payloads leave the browser |
| npm registry → build | two new packages enter the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Info Disclosure | web_vitals payload | mitigate | Send only numeric CWV + metric name/rating/id — no PII; values rounded |
| T-03-09 | Privacy (Law 25) | web-vitals pre-consent | mitigate | Listeners registered only when `consentGranted`; routed through guarded `track()` |
| T-03-SC | Tampering | npm installs (web-vitals, @next/bundle-analyzer) | mitigate | Blocking human-verify checkpoint before install; both [SUS] too-new but first-party (GoogleChrome / vercel) with no postinstall — verified on npmjs.com. Checkpoint is NOT auto-approvable |
</threat_model>

<verification>
- `bun run tsc --noEmit` exits 0.
- `bun run test src/components/WebVitalsReporter.test.tsx` passes.
- `ANALYZE=true bun run build` produces a treemap; Framer double-load finding recorded in SUMMARY.
- Runtime (verification phase): web_vitals events in GA4 only after consent; key pages hit "good" LCP<2.5s / INP<200ms / CLS<0.1.
- No onFID anywhere (grep = 0).
</verification>

<success_criteria>
Maps to ROADMAP Phase 3 success criterion 4 (MEAS-04): web-vitals RUM reports LCP/INP/CLS for key pages (consent-gated into GA4); `ANALYZE=true bun run build` confirms whether Framer Motion is double-loaded across server/client bundles. Refactor is conditional on evidence (D-11) — none performed unless the treemap shows duplication.
</success_criteria>

<output>
Create `.planning/phases/03-measurement-conversion/03-03-SUMMARY.md` when done.
</output>
