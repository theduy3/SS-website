---
phase: 02-content-schema-crawl-surface
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/app/llms.txt/route.ts
  - src/proxy.ts
  - src/proxy.test.ts
  - src/app/sitemap.ts
  - src/app/llms.txt/route.test.ts
autonomous: true
requirements: [CRAWL-01, CRAWL-02, SCHEMA-03]
must_haves:
  truths:
    - "GET /llms.txt returns HTTP 200, Content-Type text/plain; charset=utf-8, with curated EN business brief (NAP + services + hours + key-page links) — NOT a locale redirect (CRAWL-01, D-08/D-09)"
    - "/llms.txt is listed in STANDALONE_PATHS in src/proxy.ts AND src/proxy.test.ts asserts it bypasses locale routing — in the SAME plan (CRAWL-01 merge gate, D-09)"
    - "sitemap.ts emits an x-default alternate and accurate per-page lastModified (not a single new Date()) (CRAWL-02)"
    - "AggregateRating remains gated behind the existing reviews-fetch flag — never hand-authored (SCHEMA-03)"
  artifacts:
    - path: "src/app/llms.txt/route.ts"
      provides: "force-static text/plain route handler with curated business brief"
      contains: "force-static"
    - path: "src/proxy.test.ts"
      provides: "assertion that /llms.txt bypasses locale routing"
      contains: "llms.txt"
  key_links:
    - from: "src/app/llms.txt/route.ts"
      to: "src/proxy.ts STANDALONE_PATHS"
      via: "the new un-localized route MUST be registered or the proxy 404s it at runtime"
      pattern: "llms\\.txt"
    - from: "src/app/llms.txt/route.ts"
      to: "src/lib/site.ts"
      via: "NAP/services/hours sourced from site.ts single source"
      pattern: "site\\."
---

<objective>
Open the AI-agent crawl surface: a curated, force-static /llms.txt business brief, with the
proxy STANDALONE_PATHS registration + locking test that the build will NOT catch, plus sitemap
crawl hygiene (x-default + accurate lastModified) and a confirmation that AggregateRating stays
behind the live-reviews gate.

Purpose: Satisfy CRAWL-01, CRAWL-02, and verify SCHEMA-03 (no hand-authored ratings).
Output: /llms.txt route + proxy registration + proxy test + sitemap hygiene.
</objective>

## Phase Goal

**As an** AI crawling agent, **I want to** fetch /llms.txt and an accurate sitemap with a single curated business brief and clean hreflang, **so that** I can index Sans Souci's canonical facts and key pages without hitting locale redirects or stale metadata.

## Artifacts this phase produces (this plan)

- `src/app/llms.txt/route.ts` — `force-static` route handler emitting `text/plain; charset=utf-8` curated EN brief sourced from `site.ts`.
- `/llms.txt` entry added to `STANDALONE_PATHS` in `src/proxy.ts`.
- `src/proxy.test.ts` assertion proving `/llms.txt` is NOT locale-prefixed (the runtime-only failure the build can't catch — see project memory `standalone-route-proxy-coupling`).
- `src/app/sitemap.ts` hygiene: `x-default` alternate + accurate per-page `lastModified`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-schema-crawl-surface/02-CONTEXT.md
@.planning/phases/02-content-schema-crawl-surface/02-UI-SPEC.md
@.planning/codebase/CONCERNS.md
@src/lib/site.ts
@src/proxy.ts
@src/app/robots.ts
@src/app/sitemap.ts
@.planning/phases/02-content-schema-crawl-surface/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /llms.txt route handler + proxy STANDALONE registration + proxy test (MERGE GATE — one atomic unit)</name>
  <files>src/app/llms.txt/route.ts, src/proxy.ts, src/proxy.test.ts, src/app/llms.txt/route.test.ts</files>
  <read_first>
    - src/proxy.ts (STANDALONE_PATHS Set at line ~13; `if (STANDALONE_PATHS.has(pathname)) return NextResponse.next()` at ~66)
    - src/proxy.test.ts (existing "does not locale-prefix /clientportal" assertion — copy that shape for /llms.txt)
    - src/app/robots.ts (precedent for a site.ts-sourced metadata route)
    - src/lib/site.ts (name, url, booking, contact.phone/phoneHref/email/address, hours, nav, secondaryNav)
    - .planning/codebase/CONCERNS.md (standalone-route ↔ proxy coupling: build won't catch a missing entry)
  </read_first>
  <behavior>
    - GET /llms.txt → 200, header Content-Type "text/plain; charset=utf-8", body contains "Sans Souci", the street address, services list, hours, booking link, and links to /faq, /services, /laval.
    - route exports `export const dynamic = "force-static"`.
    - proxy.ts STANDALONE_PATHS includes "/llms.txt".
    - proxy.test.ts has a new assertion: a request to /llms.txt is NOT redirected to a locale-prefixed path (mirrors the /clientportal test).
    - route.test.ts asserts the 200 + content-type + "Sans Souci" body.
  </behavior>
  <action>
    Create `src/app/llms.txt/route.ts` exporting `export const dynamic = "force-static"` and a `GET` handler that returns a `Response` (or `NextResponse`) with body = a curated EN business brief and headers `{ "Content-Type": "text/plain; charset=utf-8" }`. Build the brief string from `site.ts` ONLY (D-08): business name + one-line summary ("Nail salon and spa at CF Carrefour Laval…"), NAP (full address from `site.contact.address`, phone `site.contact.phone`, email), services list, opening hours from `site.hours`, booking link (`site.url + site.booking`), and absolute links to key pages (`/en/faq`, `/en/services`, `/en/laval`). Append a single note that fr/es/ar variants exist (canonical language EN, D-08). Do NOT inline secrets; do NOT fetch external data. Then, in `src/proxy.ts`, add `"/llms.txt"` to the `STANDALONE_PATHS` Set — this is MANDATORY: without it the proxy locale-prefixes the request and the route 404s at runtime, and the build will NOT catch it (project memory standalone-route-proxy-coupling). Then, in `src/proxy.test.ts`, add an assertion modeled on the existing `/clientportal` test that a request to `/llms.txt` is returned via `NextResponse.next()` (i.e. NOT redirected to `/{locale}/llms.txt`). Also add `src/app/llms.txt/route.test.ts` asserting the handler returns status 200, the `text/plain; charset=utf-8` content type, and a body containing "Sans Souci". All three changes (route, proxy registration, proxy test) MUST land together — this is the merge gate.
  </action>
  <verify>
    <automated>bun run build && bun run test src/proxy.test.ts src/app/llms.txt/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c '"/llms.txt"' src/proxy.ts` returns 1 (STANDALONE_PATHS entry present — merge-gate half 1).
    - `git grep -c 'llms.txt' src/proxy.test.ts` returns >= 1 (locale-bypass assertion present — merge-gate half 2).
    - `git grep -c 'force-static' src/app/llms.txt/route.ts` returns 1.
    - `bun run test src/proxy.test.ts` passes including the new /llms.txt non-redirect assertion.
    - `bun run test src/app/llms.txt/route.test.ts` passes: 200 + `text/plain; charset=utf-8` + body contains "Sans Souci".
    - Route body contains the street address and links to /faq, /services, /laval: route.test asserts body matches `/faq`, `/services`, and `/laval`.
  </acceptance_criteria>
  <done>/llms.txt serves a curated force-static EN brief, is registered in STANDALONE_PATHS, and proxy.test.ts locks the locale-routing bypass — all in one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sitemap hygiene — x-default + accurate lastModified; confirm AggregateRating gate</name>
  <files>src/app/sitemap.ts</files>
  <read_first>
    - src/app/sitemap.ts (current alternates map; the single `lastModified = new Date()` to replace; /laval entry added in plan 02)
    - src/lib/seo.ts (languageAlternates already adds x-default → defaultLocale; reuse the pattern)
    - src/lib/reviews.ts (reviewsFetchedAt gate — confirm AggregateRating only emits when set; SCHEMA-03)
  </read_first>
  <behavior>
    - Every sitemap entry's alternates include an x-default key (pointing to the default-locale URL).
    - lastModified is no longer a single live `new Date()` for all pages — it is an accurate per-page date (static map, git mtime, or content-hash — Claude's call per D-09 discretion).
    - No change to AggregateRating logic; a test/assertion confirms it stays behind reviewsFetchedAt (SCHEMA-03 verification, no hand-authored rating).
  </behavior>
  <action>
    In `src/app/sitemap.ts`, add an `x-default` alternate to each entry's `alternates.languages` map pointing to the default-locale URL (mirror `languageAlternates` in seo.ts which already maps `x-default → /${defaultLocale}${route}`). Replace the blanket `lastModified = new Date()` with accurate per-page modification dates: pick ONE deterministic approach (D-09 discretion) — a static per-route date map keyed by path is acceptable and simplest; if git mtime is preferred, read it at build time. Do NOT use `new Date()` for all pages. Confirm the /laval entry (added in plan 02) also gets x-default + a real lastModified. For SCHEMA-03: do NOT add any AggregateRating logic anywhere; verify (via existing seo.ts gate `reviewsFetchedAt`) that aggregateRating stays conditional — if no test asserts this, add a short assertion to an existing seo test that organizationGraph omits aggregateRating when reviewsFetchedAt is falsy.
  </action>
  <verify>
    <automated>bun run build && bun run test 2>/dev/null; git grep -c 'x-default' src/app/sitemap.ts; grep -c 'new Date()' src/app/sitemap.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c 'x-default' src/app/sitemap.ts` returns >= 1.
    - `grep -v '^\s*//' src/app/sitemap.ts | grep -c 'new Date()'` returns 0 (no blanket live-date lastModified).
    - `bun run build` succeeds and the generated sitemap contains an `xhtml:link rel="alternate" hreflang="x-default"` for /laval and nav routes.
    - SCHEMA-03: a test asserts `organizationGraph` omits `aggregateRating` when `reviewsFetchedAt` is falsy (no hand-authored rating).
  </acceptance_criteria>
  <done>Sitemap emits x-default + accurate per-page lastModified for all routes including /laval; AggregateRating remains gated and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /llms.txt | Unauthenticated GET of a public text endpoint |
| new un-localized route → proxy | A standalone route must be explicitly exempted from locale routing or it 404s |
| site.ts NAP → /llms.txt body | Author-controlled NAP rendered as plain text |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information disclosure | /llms.txt body | mitigate | Brief is curated PUBLIC business info only (D-08): NAP, services, hours, public page links. No env vars, no secrets, no admin paths. Sourced from site.ts which contains no secrets. |
| T-02-08 | Spoofing/routing bypass | /llms.txt missing from STANDALONE_PATHS | mitigate | MANDATORY proxy registration + proxy.test.ts assertion in same plan; build cannot catch the runtime 404 (project memory). Merge-gate acceptance criteria grep both halves. |
| T-02-09 | Tampering | AggregateRating self-authored | mitigate | SCHEMA-03: rating stays behind `reviewsFetchedAt` gate; plan adds a test asserting omission when unfetched. Never hand-author rating numbers. |
| T-02-10 | DoS | force-static /llms.txt | accept | `force-static` pre-renders the route — no per-request compute, served as a static asset. |

No unmitigated HIGH. No package installs in this plan (no Package Legitimacy Gate needed).
</threat_model>

<verification>
- `bun run build` succeeds; /llms.txt pre-rendered as static text.
- `bun run test src/proxy.test.ts src/app/llms.txt/route.test.ts` passes.
- proxy.ts has /llms.txt in STANDALONE_PATHS; proxy.test.ts asserts bypass; sitemap has x-default + real lastModified; AggregateRating gate tested.
</verification>

<success_criteria>
- CRAWL-01: /llms.txt 200 force-static curated brief; proxy.test.ts asserts locale-routing bypass.
- CRAWL-02: sitemap.ts emits x-default + accurate lastModified; /laval included; link-based nav, no orphans.
- SCHEMA-03: AggregateRating only via the reviews-fetch gate, verified by test.
</success_criteria>

<output>
Create `.planning/phases/02-content-schema-crawl-surface/02-03-SUMMARY.md` when done.
</output>
