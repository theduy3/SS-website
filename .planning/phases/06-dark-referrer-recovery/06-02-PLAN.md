---
phase: 06-dark-referrer-recovery
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/api/dark-referral/route.ts
  - src/app/api/dark-referral/route.test.ts
  - src/proxy.ts
  - src/proxy.test.ts
autonomous: true
requirements: [GEO-02]
user_setup:
  - service: dokploy
    why: "The shared-secret guard (D-06) and the service-role write both read env vars. Without DARK_REFERRAL_SECRET the proxy POST and the route silently no-op (graceful degrade); rows only persist once it is set on Dokploy (and in .env.local for local verification)."
    env_vars:
      - name: DARK_REFERRAL_SECRET
        source: "Generate with `openssl rand -hex 32`. Set on Dokploy AND in .env.local for local row-write verification."
    dashboard_config:
      - task: "Add DARK_REFERRAL_SECRET to the Dokploy service env (SUPABASE_SERVICE_ROLE_KEY + NEXT_PUBLIC_SUPABASE_URL already exist)."
        location: "Dokploy → SS-website service → Environment"

must_haves:
  truths:
    - "An AI-referred request to proxy() schedules a non-blocking after() callback (D-04)"
    - "A non-AI request schedules no after() callback"
    - "A utm_source=chatgpt.com request with no Referer schedules the after() callback (D-01)"
    - "Detection runs before any early return in proxy() so bare-path requests are caught before the locale 301 (RESEARCH R-01)"
    - "Existing proxy behavior is unchanged: locale redirect, admin gate, STANDALONE_PATHS, www→bare redirect still pass their tests"
    - "POST /api/dark-referral with a wrong x-dark-referral-secret returns 401"
    - "POST /api/dark-referral no-ops (no throw) when DARK_REFERRAL_SECRET is absent"
    - "The route writes only the 4 allowlisted fields via buildInsertPayload (D-09)"
  prohibitions:
    - "proxy() must NOT await postDarkReferral (must use after() so the 301/response is never delayed — D-04)"
    - "proxy.ts must NOT import getSupabaseAdmin or @supabase/supabase-js (keeps the Supabase SDK out of the proxy bundle — D-04)"
    - "the route must NOT write created_at or any field beyond the 4 allowlisted (D-09)"
    - "the proxy matcher must NOT be changed (it already excludes /api → no loop — RESEARCH R-03)"
  artifacts:
    - path: "src/app/api/dark-referral/route.ts"
      provides: "secret-guarded Node POST route; service-role insert of the 4-field payload"
      exports: ["POST"]
    - path: "src/app/api/dark-referral/route.test.ts"
      provides: "secret-wrong → 401, secret-absent → no-op tests"
    - path: "src/proxy.ts"
      provides: "detectAiReferral call + after() dispatch + postDarkReferral helper"
      contains: "after("
    - path: "src/proxy.test.ts"
      provides: "AI-detection wiring assertions (after() called / not called)"
  key_links:
    - from: "src/proxy.ts"
      to: "src/lib/dark-referral.ts"
      via: "imports detectAiReferral + DarkReferralRow (from Plan 01)"
      pattern: "from \"@/lib/dark-referral\""
    - from: "src/proxy.ts"
      to: "src/app/api/dark-referral/route.ts"
      via: "postDarkReferral fetches ${origin}/api/dark-referral with x-dark-referral-secret header"
      pattern: "/api/dark-referral"
    - from: "src/app/api/dark-referral/route.ts"
      to: "src/lib/supabase.ts"
      via: "getSupabaseAdmin().from(DARK_REFERRALS_TABLE).insert(row) (from Plan 01)"
      pattern: "DARK_REFERRALS_TABLE"
---

<objective>
Wire the request path: a secret-guarded internal Node route that service-role-
inserts the 4-field row, and proxy.ts detection that fires a non-blocking
after() POST to it on every AI-referred request — without delaying the page
response or the locale 301, and without bundling the Supabase SDK into the
proxy. Imports the detection lib + DARK_REFERRALS_TABLE from Plan 01.

Purpose: Completes GEO-02 end-to-end — an AI-referred request now produces one
PII-free Supabase row, verified by route tests and proxy wiring tests.
Output: 1 new route, 1 new route test, proxy.ts + proxy.test.ts modified.
</objective>

<execution_context>
@/Users/theduy/Repo/SS-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/theduy/Repo/SS-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dark-referrer-recovery/06-CONTEXT.md
@.planning/phases/06-dark-referrer-recovery/06-RESEARCH.md
@.planning/phases/06-dark-referrer-recovery/06-PATTERNS.md
@.planning/phases/06-dark-referrer-recovery/06-01-SUMMARY.md

# Project guard: Next.js 16 differs from training data; src/proxy.ts is the
# renamed middleware. Trust RESEARCH for after()/proxy/runtime facts.
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- API route `src/app/api/dark-referral/route.ts` exporting `POST`
- route test `src/app/api/dark-referral/route.test.ts`
- in `src/proxy.ts`: module-level `postDarkReferral(row, origin)` helper +
  the detectAiReferral/after() block at the top of `proxy()`
- new proxy-detection assertions in `src/proxy.test.ts`
- consumes env var `DARK_REFERRAL_SECRET` (D-06); reuses
  `SUPABASE_SERVICE_ROLE_KEY` + `NEXT_PUBLIC_SUPABASE_URL`
Consumes from Plan 01: detectAiReferral, DarkReferralRow, buildInsertPayload,
DARK_REFERRALS_TABLE.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Internal secret-guarded log route (D-05/D-06/D-09)</name>
  <files>src/app/api/dark-referral/route.ts, src/app/api/dark-referral/route.test.ts</files>
  <read_first>
    - src/app/api/contact/route.ts — the Node route-handler analog (body parse, env-guarded branch, NextResponse.json)
    - src/lib/supabase.ts — getSupabaseAdmin() + DARK_REFERRALS_TABLE (added in Plan 01)
    - src/lib/dark-referral.ts — buildInsertPayload + DarkReferralRow (Plan 01)
    - .planning/phases/06-dark-referrer-recovery/06-PATTERNS.md — section "src/app/api/dark-referral/route.ts (NEW)" (lines ~176–229)
    - .planning/phases/06-dark-referrer-recovery/06-RESEARCH.md — Pattern 3, R-03 (Node runtime), R-04 (service-role insert), Pitfall 3 (missing-secret → no-op, not 401)
  </read_first>
  <behavior>
    - POST with header x-dark-referral-secret !== DARK_REFERRAL_SECRET → 401
    - POST when DARK_REFERRAL_SECRET is unset → responds ok, no throw (no-op)
    - POST with correct secret + valid body, Supabase unconfigured → responds ok, no throw
    - POST with non-JSON body (correct secret) → 400
    - the row passed to insert has exactly the 4 allowlisted keys (built via buildInsertPayload) — no created_at, no extra body fields leak through
  </behavior>
  <action>
    Create `src/app/api/dark-referral/route.ts` modeled on contact/route.ts,
    exporting an async `POST(request)`. Order per D-06 / D-05 / D-09:
    1. Read `process.env.DARK_REFERRAL_SECRET`. If absent → return ok JSON
       (silent no-op, graceful degrade — NOT a 401; Pitfall 3).
    2. Compare the `x-dark-referral-secret` request header to the env secret;
       on mismatch return 401. (Simple string compare is acceptable here per
       D-06; the value is a server-injected shared secret, not user-derived.)
    3. Parse the JSON body in try/catch → 400 on failure.
    4. Destructure ONLY ai_source, referrer_host, path, utm_source from the body
       and pass through buildInsertPayload (Plan 01) — this drops any extra
       fields the body might carry (the route-layer half of the D-09 invariant).
       Do NOT add the DB-default timestamp column to the payload (Postgres sets
       it via DEFAULT now()).
    5. `const db = getSupabaseAdmin();` — if null, return ok (no-op, Supabase
       unconfigured). Otherwise `db.from(DARK_REFERRALS_TABLE).insert(row)`.
       Logging is best-effort: do not surface insert errors to the caller.
    Do NOT add a Zod schema (this is an internal trusted-after-secret route, not
    a public user form). Then create `route.test.ts` covering every <behavior>
    case. For the Supabase-unconfigured + insert paths, mock the supabase module
    (getSupabaseAdmin) as in 06-PATTERNS.md. Construct the request with
    `new Request("http://localhost/api/dark-referral", {method:"POST", headers, body})`.
    Run via `bun run test` — NEVER `bun test`.
  </action>
  <verify>
    <automated>bun run test src/app/api/dark-referral/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/app/api/dark-referral/route.test.ts` passes all cases
    - `grep -c "x-dark-referral-secret" src/app/api/dark-referral/route.ts` >= 1
    - `grep -c "buildInsertPayload" src/app/api/dark-referral/route.ts` >= 1 (route routes through the allowlist wrapper)
    - `grep -c "DARK_REFERRALS_TABLE" src/app/api/dark-referral/route.ts` >= 1
    - `grep -v '^//' src/app/api/dark-referral/route.ts | grep -c "created_at"` == 0 (route never writes created_at — prohibition)
    - test file asserts a 401 on wrong secret and an ok/no-throw on absent secret
  </acceptance_criteria>
  <done>
    Route returns 401 on wrong secret, no-ops on absent secret or unconfigured
    Supabase, parses body with 400 fallback, and inserts only the 4-field
    buildInsertPayload row into DARK_REFERRALS_TABLE via service-role; route
    tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: proxy.ts detection wiring + after() dispatch + proxy tests</name>
  <files>src/proxy.ts, src/proxy.test.ts</files>
  <read_first>
    - src/proxy.ts — current structure: proxy() at line 38, www-redirect / admin gate / STANDALONE_PATHS / locale routing; matcher at line 85 (already excludes /api)
    - src/proxy.test.ts — existing req() helper + locale-routing assertions to preserve
    - src/lib/dark-referral.ts — detectAiReferral + DarkReferralRow (Plan 01)
    - .planning/phases/06-dark-referrer-recovery/06-PATTERNS.md — section "src/proxy.ts (MODIFY)" (lines ~25–83) and "src/proxy.test.ts (MODIFY)" (lines ~361–414)
    - .planning/phases/06-dark-referrer-recovery/06-RESEARCH.md — R-01 (detection must be first statement; bare-path Referer), R-02 (after() supported in Proxy on Node/Dokploy), R-03 (URL construction + no loop), Open Question 1 (loopback vs origin)
  </read_first>
  <behavior>
    - proxy(reqWithHeaders("/about", {referer:"https://chatgpt.com/c/abc"})) → after() called once; response still locale-redirects to /.../about
    - proxy(reqWithHeaders("/about", {referer:"https://google.com/"})) → after() NOT called; still locale-redirects
    - proxy(req("/about?utm_source=chatgpt.com")) → after() called once (D-01)
    - existing locale-routing / STANDALONE_PATHS / admin tests still pass unchanged
  </behavior>
  <action>
    Modify `src/proxy.ts` per D-04 / D-06 / RESEARCH R-01/R-02/R-03:
    1. Extend the next/server import to include `after`; add
       `import { detectAiReferral, type DarkReferralRow } from "@/lib/dark-referral";`.
       Do NOT import getSupabaseAdmin or any @supabase client here (keeps the SDK
       out of the proxy bundle — prohibition).
    2. Add a module-level `async function postDarkReferral(row, origin)` above
       proxy(): read DARK_REFERRAL_SECRET; if absent → return (silent no-op).
       Otherwise fetch `${origin}/api/dark-referral` with method POST,
       content-type json, header `x-dark-referral-secret: <secret>`, body
       JSON.stringify(row), all wrapped in try/catch that swallows errors (never
       crash a render). For the POST target URL use a configurable base: read an
       optional `process.env.DARK_REFERRAL_ORIGIN` and fall back to the passed
       `origin` (request.nextUrl.origin) — this resolves RESEARCH Open Question 1
       (loopback vs public origin in Dokploy) without leaving it ambiguous;
       default behavior uses the request origin, overridable to
       http://localhost:3000 if the container can't reach its public host.
    3. As the FIRST statements inside proxy() — before the www-redirect and
       every other return (R-01: Referer rides the first bare-path request) —
       call detectAiReferral(request.headers.get("referer"),
       request.nextUrl.searchParams.get("utm_source"), request.nextUrl.pathname).
       If it returns a row, `after(() => void postDarkReferral(row, request.nextUrl.origin));`.
       Never await it (prohibition: must not delay the 301).
    4. Leave the matcher unchanged (it already excludes /api → no loop).
    Then modify `src/proxy.test.ts`: add a `reqWithHeaders(path, headers)` helper
    and mock `next/server` so `after` is a vi.fn() capturing calls (pattern in
    06-PATTERNS.md). Add the 3 detection-wiring cases in <behavior>; keep all
    existing locale/standalone assertions intact and passing.
    Run via `bun run test` — NEVER `bun test`.
  </action>
  <verify>
    <automated>bun run test src/proxy.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/proxy.test.ts` passes — both new detection cases AND all pre-existing locale/standalone cases green
    - `grep -c "after(" src/proxy.ts` >= 1 (after() dispatch present)
    - `grep -cE "await postDarkReferral" src/proxy.ts` == 0 (never awaited — prohibition)
    - `grep -c "getSupabaseAdmin\|@supabase" src/proxy.ts` == 0 (SDK kept out of proxy bundle — prohibition)
    - `grep -c "detectAiReferral" src/proxy.ts` >= 1
    - the matcher line `"/((?!_next|api|.*\\..*).*)"` is unchanged in src/proxy.ts
    - `bun run typecheck` exits 0
  </acceptance_criteria>
  <done>
    proxy() calls detectAiReferral as its first statement and dispatches a
    non-blocking after() POST on a match; the POST helper degrades silently
    without the secret; the SDK is not imported into proxy; the matcher is
    untouched; new + existing proxy tests and typecheck all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/dark-referral | Route is reachable (proxy matcher excludes /api); untrusted POSTs could forge rows |
| AI client → proxy | Untrusted Referer/utm_source header + query reach detectAiReferral |
| proxy → internal route | proxy injects the shared secret; route verifies before writing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Spoofing/Tampering | /api/dark-referral (forged/spam rows) | mitigate | D-06 x-dark-referral-secret header verified in route; wrong secret → 401; proxy injects it from env |
| T-06-06 | Information Disclosure | insert payload (PII leak via extra body fields) | mitigate | route destructures only 4 fields + buildInsertPayload drops extras (D-09); created_at DB-set |
| T-06-07 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY / DARK_REFERRAL_SECRET | mitigate | both server-only; proxy.ts imports no supabase client; secret read from process.env only |
| T-06-08 | Denial of Service | slow Supabase blocking page render | mitigate | after() dispatch post-response (R-02); fetch wrapped in try/catch, never awaited in proxy |
| T-06-09 | Denial of Service | unbounded logging at high traffic | accept | current salon traffic is low; sampling/retention deferred (CONTEXT deferred ideas) |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH: "No new npm dependencies"); no install tasks → no legitimacy gate needed |
</threat_model>

<verification>
- `bun run test src/proxy.test.ts src/app/api/dark-referral/route.test.ts` — wiring + route gates green
- `bun run test` (full suite) — no regression in existing proxy/analytics/popup tests
- `bun run typecheck` — clean
- `grep -c "getSupabaseAdmin\|@supabase" src/proxy.ts` == 0 — SDK absent from proxy
- `grep -cE "await postDarkReferral" src/proxy.ts` == 0 — POST never blocks the response
</verification>

<success_criteria>
- An AI-referred request (Referer host OR utm_source in the v1 set) schedules a non-blocking after() POST; a non-AI request does not (GEO-02 capture, D-01/D-04)
- The internal route rejects forged POSTs (wrong secret → 401) and writes only the 4 PII-free fields via service-role (D-05/D-06/D-09)
- Existing proxy behavior (locale, admin, standalone, www) is fully preserved
- Detection runs before the locale 301 so bare-path AI referrals are caught (R-01)
</success_criteria>

<output>
Create `.planning/phases/06-dark-referrer-recovery/06-02-SUMMARY.md` when done.
</output>
