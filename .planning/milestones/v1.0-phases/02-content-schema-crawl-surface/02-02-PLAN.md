---
phase: 02-content-schema-crawl-surface
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/[lang]/laval/page.tsx
  - src/dictionaries/en.json
  - src/dictionaries/fr.json
  - src/dictionaries/es.json
  - src/dictionaries/ar.json
  - src/app/sitemap.ts
  - src/app/[lang]/laval/page.test.tsx
autonomous: true
requirements: [CONTENT-03, SCHEMA-01, SCHEMA-02]
must_haves:
  truths:
    - "A localized /[lang]/laval route renders in all 4 locales (en/fr/es/ar) under normal [lang] routing — NOT a standalone path (CONTENT-03, D-05)"
    - "The Laval page opens with a 40-60 word answer-first lead, then location/parking/transit/landmark facts, then a 3-5 item Laval-specific FAQ (CONTENT-03, D-06)"
    - "The Laval page emits FAQPage + BreadcrumbList JSON-LD in raw SSR HTML, with FAQPage fed from dict.laval.faq.items (SCHEMA-01/02)"
    - "All 4 locale variants ship atomically with reciprocal hreflang and a sitemap entry (D-06)"
    - "The local page is Laval (route /laval, dict.laval.*, CF Carrefour Laval facts) — NOT Montreal; this reconciles the Phase-1 D-02 identity drift (D-07)"
  artifacts:
    - path: "src/app/[lang]/laval/page.tsx"
      provides: "Laval local page: lead + facts dl + FAQ accordion + CTA + schema"
      contains: "dict.laval"
    - path: "src/dictionaries/en.json"
      provides: "laval block: lead, heading, intro, faqHeading, facts, faq.items"
      contains: "\"laval\""
  key_links:
    - from: "src/app/[lang]/laval/page.tsx"
      to: "src/lib/seo.ts faqPageGraph"
      via: "dict.laval.faq.items feeds both schema and Accordion"
      pattern: "faqPageGraph\\(dict\\.laval\\.faq\\.items\\)"
    - from: "src/app/sitemap.ts"
      to: "/laval route"
      via: "sitemap entry for the new localized route"
      pattern: "laval"
---

<objective>
Ship the dedicated Laval local page as a localized route across all 4 locales, with an
answer-first lead, location facts (CF Carrefour Laval / parking / transit / landmarks), a
Laval-specific FAQ carrying its own FAQPage schema, and a sitemap entry — establishing the
local-citation signal AI engines and Google use for "near me" answers.

Purpose: Satisfy CONTENT-03, and extend SCHEMA-01/02 to the new route.
Output: New /[lang]/laval page, laval dictionary block in 4 locales, sitemap entry.
</objective>

## Phase Goal

**As a** local searcher (or an AI answering a "nail salon in Laval" query), **I want to** find a Laval-specific page with location facts and local FAQ schema in server-rendered HTML, **so that** Sans Souci is surfaced as the local answer with verifiable structured data.

## Artifacts this phase produces (this plan)

- `dict.laval` block in all 4 locales: `lead`, `heading`, `intro`, `faqHeading`, location `facts` (address/parking/transit/landmarks), and `faq.items` (3-5 Q/As).
- New `src/app/[lang]/laval/page.tsx` localized route (single `laval` slug, NOT in STANDALONE_PATHS per D-05).
- Sitemap entry for `/laval` with reciprocal hreflang.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-schema-crawl-surface/02-CONTEXT.md
@.planning/phases/02-content-schema-crawl-surface/02-UI-SPEC.md
@src/lib/seo.ts
@src/lib/site.ts
@src/app/[lang]/faq/page.tsx
@src/app/sitemap.ts
@.planning/phases/02-content-schema-crawl-surface/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the laval dictionary block (all 4 locales)</name>
  <files>src/dictionaries/en.json, src/dictionaries/fr.json, src/dictionaries/es.json, src/dictionaries/ar.json</files>
  <read_first>
    - src/lib/site.ts (address.street "3035 Boulevard le Carrefour, Entrée 6", city "Laval", region "QC", geo, hours)
    - .planning/phases/02-content-schema-crawl-surface/02-UI-SPEC.md § "Laval Local Page" and Copywriting Contract (laval keys)
    - src/dictionaries/en.json (faq.items shape {q,a}; serviceDetails[id].faq shape for facts/dl precedent)
  </read_first>
  <behavior>
    - en.json gains a top-level `laval` object: lead (40-60 words), heading, intro (1 sentence), faqHeading, facts (array of {term, detail} for address/parking/transit/landmarks), faq.items (3-5 {q,a}).
    - fr/es/ar mirror every laval key with translated content and IDENTICAL faq.items length.
    - All facts sourced from site.ts NAP (CF Carrefour Laval, Entrée 6) — no invented landmarks beyond verifiable ones.
  </behavior>
  <action>
    Per D-10 draft EN first then translate. Add a top-level `laval` object to en.json with: `lead` (40-60 word direct-answer about the Laval location), `heading` (e.g. "LAVAL"), `intro` (one factual sentence, neighborhood + studio), `faqHeading`, `facts` (array of objects `{term, detail}` covering address, parking at CF Carrefour Laval Entrée 6, transit access, nearby landmarks — terms become `<dt>`, details become `<dd>`), and `faq` with an `items` array of 3-5 `{q, a}` objects (Laval-specific: location, parking, transit, walk-in availability, landmarks). Mirror the entire `laval` block into fr.json, es.json, ar.json with translations and the same `faq.items` length. Source every fact from site.ts; do not invent unverifiable claims.
  </action>
  <verify>
    <automated>node -e "const L=['en','fr','es','ar'].map(l=>require('./src/dictionaries/'+l+'.json')); const c=L.map(d=>d.laval.faq.items.length); if(new Set(c).size!==1) throw new Error('laval faq count mismatch '+c); if(c[0]<3||c[0]>5) throw new Error('laval faq out of 3-5: '+c[0]); for(const d of L){const w=String(d.laval.lead).trim().split(/\\s+/).length; if(w<40||w>60) throw new Error('laval.lead words '+w); if(!Array.isArray(d.laval.facts)||d.laval.facts.length<3) throw new Error('laval.facts too few');} console.log('laval dict OK',c[0])"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e` check exits 0: laval.faq.items equal across locales and within 3-5; laval.lead 40-60 words; facts >= 3.
    - `git grep -c '"laval"' src/dictionaries/en.json` returns >= 1.
    - All 4 locale files contain the laval block (`for l in en fr es ar; do node -e "require('./src/dictionaries/$l.json').laval.heading"; done` exits 0).
  </acceptance_criteria>
  <done>The laval block exists in all 4 dictionaries with matching faq counts (3-5), a 40-60 word lead, and >=3 location facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the /[lang]/laval localized route with schema + facts + FAQ</name>
  <files>src/app/[lang]/laval/page.tsx, src/app/[lang]/laval/page.test.tsx</files>
  <read_first>
    - src/app/[lang]/faq/page.tsx (generateMetadata + pageMetadata + JsonLd + PageHeader + Accordion pattern)
    - src/app/[lang]/services/[slug]/page.tsx (facts <dl> pattern, bottom CTA pattern, dirFor back-link)
    - .planning/phases/02-content-schema-crawl-surface/02-UI-SPEC.md § "Laval Local Page" (exact section order + classes)
    - src/lib/seo.ts (pageMetadata, faqPageGraph, breadcrumbGraph, organizationGraph)
  </read_first>
  <behavior>
    - generateMetadata returns pageMetadata(lang, "/laval", {...}) → reciprocal hreflang across 4 locales.
    - Page render order: answer-first lead <p> → PageHeader(heading, intro) → facts <dl> section → bg-fog FAQ Accordion section → CTA section.
    - The lead <p> is a plain element, NOT wrapped in <Reveal>/motion — Framer Motion opacity:0 would hide it on first paint and from no-JS AI crawlers (RESEARCH Pitfall 3); the lead must be SSR-visible (CONTENT-03).
    - Emits faqPageGraph(dict.laval.faq.items) + breadcrumbGraph([home, laval]) JsonLd in SSR.
    - Invalid lang → notFound().
    - Test asserts SSR output contains "carrefour" and "@type":"FAQPage".
  </behavior>
  <action>
    Create `src/app/[lang]/laval/page.tsx` following the faq-page structure. Export `generateMetadata` returning `pageMetadata(lang, "/laval", { title: dict.meta... or dict.laval.heading-derived, description: dict.laval.intro })` so hreflang is reciprocal across all 4 locales (single shared `laval` slug — use the route form, NOT routeByLocale, per D-05). Default export `LavalPage`: guard `if (!isLocale(lang)) notFound()`, load `dict = await getDictionary(lang)`. Render in this order per the UI-SPEC: (1) `<JsonLd data={faqPageGraph(dict.laval.faq.items)} />` and `<JsonLd data={breadcrumbGraph(lang, [{name: dict.nav.home, route: ""}, {name: dict.laval.heading, route: "/laval"}])} />`; (2) the answer-first lead as a plain `<p className="mx-auto max-w-3xl px-6 pt-8 text-lg leading-relaxed text-mocha md:pt-12">{dict.laval.lead}</p>` BEFORE PageHeader — NOT wrapped in `<Reveal>`/any Framer Motion container (it sets opacity:0 server-side and hides the lead until hydration; the answer block must be visible on first paint and to no-JS AI crawlers — RESEARCH Pitfall 3 / PATTERNS); (3) `<PageHeader title={dict.laval.heading} intro={dict.laval.intro} />`; (4) a facts `<section className="mx-auto max-w-3xl px-6 py-20 md:py-28">` with a `<dl>` mapping `dict.laval.facts` to `<dt className="text-sm uppercase tracking-wide text-mocha">{f.term}</dt><dd className="mt-1 leading-relaxed text-mocha">{f.detail}</dd>`; (5) a `<section className="bg-fog">` wrapping `<h2 className="text-2xl text-espresso md:text-3xl">{dict.laval.faqHeading}</h2>` + `<Accordion items={dict.laval.faq.items} />`; (6) a centered CTA `<section>` with `<Button href={`/${lang}${site.booking}`}>{dict.cta.book}</Button>` and a phone `<a href={site.contact.phoneHref}>`. Feed `dict.laval.faq.items` to BOTH `faqPageGraph` and `<Accordion>` (single source, SCHEMA-02). If a back-link arrow is used, use `{dirFor(lang) === "rtl" ? "→" : "←"}`. This page is localized — do NOT add it to STANDALONE_PATHS (D-05).
  </action>
  <verify>
    <automated>bun run build && bun run test src/app/[lang]/laval/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/app/[lang]/laval/page.test.tsx` passes: SSR render output contains case-insensitive "carrefour" and the literal `"@type":"FAQPage"`.
    - Test asserts the lead `<p>` text precedes the PageHeader title in document order.
    - The lead `<p>` is NOT motion-wrapped: it is a direct child of the returned fragment, not nested inside a `<Reveal>`/motion element; SSR test asserts the lead text has no `opacity:0`/style-hidden ancestor in render output (RESEARCH Pitfall 3 — visible first paint).
    - `git grep -c 'faqPageGraph(dict.laval.faq.items)' "src/app/[lang]/laval/page.tsx"` returns 1 (single source).
    - `git grep -c '/laval' src/proxy.ts` returns 0 (NOT in STANDALONE_PATHS, D-05).
    - `bun run build` generates the route for all 4 locales.
  </acceptance_criteria>
  <done>/[lang]/laval renders in 4 locales with lead → header → facts → FAQ → CTA, emits FAQPage+Breadcrumb schema from a single source, and is reachable via normal locale routing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add /laval to sitemap with reciprocal hreflang</name>
  <files>src/app/sitemap.ts</files>
  <read_first>
    - src/app/sitemap.ts (existing navEntries/secondaryEntries/serviceEntries/comparisonEntries structure and alternates pattern)
    - src/lib/i18n.ts (locales array, defaultLocale for x-default)
  </read_first>
  <behavior>
    - sitemap output includes one URL per locale for /laval with reciprocal alternates (languages map across all 4 locales).
    - The /laval entry follows the same shape as existing nav entries (loc + alternates.languages).
  </behavior>
  <action>
    Add `/laval` to the sitemap generation in `src/app/sitemap.ts`. Reuse the existing per-locale entry pattern (the one that builds `${site.url}/${l}${path}` with `alternates.languages`) so `/laval` emits one entry per locale with reciprocal hreflang across en/fr/es/ar. If the sitemap has a list of locale-agnostic paths driving nav/secondary entries, add `"/laval"` there rather than hand-rolling a one-off block; otherwise append a dedicated laval entry block matching the existing shape. Do NOT yet touch x-default / lastModified hygiene — that is plan 03's scope (avoid double-editing the same hygiene logic).
  </action>
  <verify>
    <automated>bun run build && git grep -c 'laval' src/app/sitemap.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c 'laval' src/app/sitemap.ts` returns >= 1.
    - `bun run build` succeeds and produces sitemap with a /laval URL per locale (verify via the sitemap test if one exists, else `bun run build` output has no sitemap error).
    - No duplicate /laval entries (single source path entry, not 4 hand-written blocks).
  </acceptance_criteria>
  <done>The sitemap emits /laval for all 4 locales with reciprocal hreflang, using the existing entry pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dictionary laval copy → JSON-LD | Author strings flow into `<JsonLd>` (innerHTML) |
| new /laval route → proxy locale routing | A new localized route must remain inside [lang] routing, not bypass it |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering/XSS | faqPageGraph emission on /laval | mitigate | Schema flows through existing `<JsonLd>` which escapes `<`; do not hand-build script tags. |
| T-02-05 | Elevation/routing bypass | New /laval route + proxy STANDALONE_PATHS | mitigate | /laval is localized — it MUST stay OUT of STANDALONE_PATHS so it goes through normal locale routing (D-05). Acceptance criterion greps proxy.ts to confirm /laval absent. |
| T-02-06 | Information disclosure | Laval location facts | accept | Public NAP/location info from site.ts; no secrets. |

No unmitigated HIGH. No package installs.
</threat_model>

<verification>
- `bun run build` succeeds; /laval generated for all 4 locales.
- `bun run test src/app/[lang]/laval/page.test.tsx` passes.
- laval block present in 4 dicts; sitemap has /laval; /laval NOT in proxy STANDALONE_PATHS.
</verification>

<success_criteria>
- CONTENT-03: one Laval local page exists in all 4 locales with neighborhood FAQ signals, server-rendered.
- SCHEMA-01/02: /laval emits FAQPage + Breadcrumb from single-source dict.laval.faq.items.
</success_criteria>

<output>
Create `.planning/phases/02-content-schema-crawl-surface/02-02-SUMMARY.md` when done.
</output>
