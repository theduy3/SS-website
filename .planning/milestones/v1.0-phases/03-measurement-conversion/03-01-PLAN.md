---
phase: 03-measurement-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analytics.ts
  - src/lib/consent.ts
  - src/lib/site.ts
  - src/components/Analytics.tsx
  - src/components/ConsentBar.tsx
  - src/app/[lang]/layout.tsx
  - src/dictionaries/en.json
  - src/dictionaries/fr.json
  - src/dictionaries/es.json
  - src/dictionaries/ar.json
  - src/components/ConsentBar.test.tsx
autonomous: true
requirements: [MEAS-01]
user_setup:
  - service: ga4
    why: "GA4 property + measurement ID + AI-referrer channel group (MEAS-01). Claude cannot reach the GA4 admin console; the channel group does not backfill so it must exist before live traffic."
    env_vars:
      - name: NEXT_PUBLIC_GA_ID
        source: "GA4 Admin -> Data streams -> Web stream -> Measurement ID (format G-XXXXXXX). Set in Dokploy at BUILD time (NEXT_PUBLIC_ vars inline at build for static pages)."
    dashboard_config:
      - task: "Create the 'AI Assistants' custom channel group with the regex Claude drafts (see <objective> AI-referrer regex), BEFORE deploying to live traffic"
        location: "GA4 Admin -> Data display -> Channel groups -> New channel group"
      - task: "Flag generate_lead, phone_click, book_cta_click as Key events (conversions) — wired in plan 03-02"
        location: "GA4 Admin -> Events -> Key events"

must_haves:
  truths:
    - "A first-time visitor on /en sees an Accept/Decline consent bar after the page hydrates"
    - "Clicking Accept sets ss_consent=granted and calls gtag consent update to analytics_storage:granted"
    - "Clicking Decline sets ss_consent=denied and leaves analytics_storage denied (no gtag consent update)"
    - "On reload after a decision the consent bar does not appear (cookie present)"
    - "When NEXT_PUBLIC_GA_ID is unset the build emits no GA4 script tags (safe no-op build)"
    - "analytics_storage defaults to denied before gtag.js loads (Consent Mode v2 ordering)"
  artifacts:
    - path: "src/lib/analytics.ts"
      provides: "Guarded track() event helper + grantConsent() consent-update helper"
      exports: ["track", "grantConsent", "GaEvent"]
    - path: "src/components/Analytics.tsx"
      provides: "Consent-default-denied stub (pre-gtag) + env-gated GA4 next/script loader"
    - path: "src/components/ConsentBar.tsx"
      provides: "JS-mounted Accept/Decline bar; writes ss_consent; calls grantConsent on Accept"
    - path: "src/lib/consent.ts"
      provides: "ss_consent cookie helpers (server read via cookies(), client write)"
    - path: "src/lib/site.ts"
      contains: "established: 2024"
  key_links:
    - from: "src/components/ConsentBar.tsx"
      to: "src/lib/analytics.ts"
      via: "grantConsent() called on Accept"
      pattern: "grantConsent\\("
    - from: "src/app/[lang]/layout.tsx"
      to: "src/components/Analytics.tsx"
      via: "Analytics mounted in body"
      pattern: "<Analytics"
    - from: "src/app/[lang]/layout.tsx"
      to: "src/lib/consent.ts"
      via: "SSR cookie read passes consentKnown prop to ConsentBar"
      pattern: "ss_consent|readConsent"
  prohibitions:
    - "ConsentBar markup MUST NOT appear in SSR HTML (JS-mounted only — UI-SPEC line 155)"
    - "Button.tsx MUST NOT receive an onClick prop (UI-SPEC line 174, D-06) — consent buttons are native <button>"
    - "GA4 gtag.js MUST NOT load before the consent-default-denied stub (Consent Mode v2 ordering, Pitfall 1)"
    - "No new Date() / dynamic time in any added code (deterministic SSR)"
    - "No PII or form values in any gtag payload (Security V7/Info-Disclosure)"
---

<objective>
Stand up the measurement spine: GA4 (env-gated `next/script`) wired behind Consent Mode v2 default-denied, a 4-locale Accept/Decline consent bar that flips `analytics_storage` on opt-in, the guarded `track()`/`grantConsent()` helpers every later event flows through, the `ss_consent` cookie (SSR-read / client-write), and the `established: 2024` field on `site.ts`. This is the vertical slice "a visitor lands, sees the consent bar, accepts, and GA4 begins recording" — MEAS-01.

Purpose: GA4 channel groups do not backfill, so the analytics pipeline (script + consent + helper) must be correct and live before any new page ships. Consent ordering is the load-bearing correctness gate (Law 25, D-02 Consent Mode v2 default-denied; Decline keeps storage denied with no granular toggles).
Output: `lib/analytics.ts`, `lib/consent.ts`, `Analytics.tsx`, `ConsentBar.tsx`, consent dictionary keys in 4 locales, `site.established`, and the layout mounts.

AI-referrer regex (Claude drafts; USER pastes into GA4 Admin -> Data display -> Channel groups -> New channel group -> channel name "AI Assistants", condition "Session source matches regex"):
`.*(chatgpt|openai|perplexity|claude\.ai|gemini\.google|copilot\.microsoft|bing\.com/chat).*`
This is a best-effort draft (D-03, A1); the user validates against GA4 Realtime -> Acquisition after the first AI-referred sessions and refines. It MUST be created before the first new page goes live (no backfill — STATE.md blocker).
</objective>

<execution_context>
@/Users/theduy/Repo/SS-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/theduy/Repo/SS-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-measurement-conversion/03-CONTEXT.md
@.planning/phases/03-measurement-conversion/03-RESEARCH.md
@.planning/phases/03-measurement-conversion/03-PATTERNS.md
@.planning/phases/03-measurement-conversion/03-UI-SPEC.md
</context>

<artifacts_produced>
This plan creates / extends the following symbols (downstream plans depend on them):
- `src/lib/analytics.ts` — exports `track(event, params)`, `grantConsent()`, and the `GaEvent` union type (`"phone_click" | "book_cta_click" | "generate_lead" | "web_vitals"`).
- `src/lib/consent.ts` — exports `readConsent()` (server, `cookies()`), `writeConsent(value)` (client, `document.cookie`); cookie name constant `ss_consent`.
- `src/components/Analytics.tsx` — `<Analytics />` (consent-default stub + env-gated GA4 loader; reads `NEXT_PUBLIC_GA_ID`).
- `src/components/ConsentBar.tsx` — `<ConsentBar dict locale consentKnown />`.
- `src/lib/site.ts` — new `established: 2024` field on the `site` object.
- `src/dictionaries/{en,fr,es,ar}.json` — new `consent` namespace (`body`, `accept`, `decline`, `ariaLabel`).
- Env wiring: `NEXT_PUBLIC_GA_ID` (read in `Analytics.tsx`, Dokploy-set, public-by-design).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: track()/grantConsent() helper, ss_consent cookie helpers, site.established</name>
  <files>src/lib/analytics.ts, src/lib/consent.ts, src/lib/site.ts</files>
  <read_first>
    - src/lib/analytics.ts (new — does not exist yet)
    - src/lib/reviews.ts (analog: small typed lib module shape, named exports + guard constant — PATTERNS line 200-201)
    - src/lib/session.ts (analog: server cookie read via next/headers cookies() — PATTERNS line 226-230; cite lines 2, 42-43)
    - src/components/LocaleSwitch.tsx (analog: client cookie WRITE via document.cookie — cite line 80, PATTERNS line 308)
    - src/lib/site.ts (file being modified — add established field to the existing exported site object)
    - .planning/phases/03-measurement-conversion/03-RESEARCH.md §2 (track/grantConsent signatures), §5
  </read_first>
  <behavior>
    - track("phone_click") with window.gtag undefined → no-op, does not throw
    - track("generate_lead", {method:"contact_form"}) with window.gtag defined → calls window.gtag("event","generate_lead",{method:"contact_form"})
    - track called when typeof window === "undefined" (server) → no-op, does not throw
    - grantConsent() with gtag defined → calls window.gtag("consent","update",{analytics_storage:"granted"})
    - readConsent() returns "granted" | "denied" | undefined from the ss_consent cookie
  </behavior>
  <action>
    Create `src/lib/analytics.ts` exporting the `GaEvent` union (`"phone_click" | "book_cta_click" | "generate_lead" | "web_vitals"`), `track(event: GaEvent, params?: Record<string, unknown>): void`, and `grantConsent(): void`. `track` must guard on `typeof window === "undefined"` then `typeof window.gtag !== "function"` and no-op otherwise before calling `window.gtag("event", event, params)`. `grantConsent` calls `window.gtag("consent","update",{ analytics_storage: "granted" })` behind the same window/gtag guard. Add the `declare global { interface Window { gtag?: (...args: unknown[]) => void } }` augmentation. Param shapes are Claude's discretion per D-04; send NO PII. Mirror the terse module shape of `reviews.ts` (no class).
    Create `src/lib/consent.ts` exporting the cookie name constant (`ss_consent`), a server `readConsent()` that does `const c = await cookies(); return c.get("ss_consent")?.value as "granted"|"denied"|undefined` (import `cookies` from `next/headers`, same as `session.ts:2`), and a client `writeConsent(value: "granted"|"denied")` that sets `document.cookie = "ss_consent=" + value + ";path=/;max-age=31536000;samesite=lax"` (copy the form from `LocaleSwitch.tsx:80`; SameSite=Lax, NO Secure flag — non-sensitive per UI-SPEC line 176).
    Modify `src/lib/site.ts`: add `established: 2024` as a new field on the existing `site` object (immutable add — extend the literal, do not mutate at runtime). Place it near `reviews`/identity facts with a one-line comment that it feeds the static "Since 2024" trust marker (D-08).
  </action>
  <verify>
    <automated>bun run tsc --noEmit 2>&1 | grep -E "analytics|consent|site" || echo "no type errors in new lib files"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/analytics.ts` source contains `typeof window.gtag !== "function"` (guard) and `window.gtag("event"` (fire).
    - `src/lib/analytics.ts` exports `track`, `grantConsent`, and type `GaEvent` (grep: `export function track`, `export function grantConsent`, `export type GaEvent`).
    - `src/lib/consent.ts` server read uses `cookies()` from `next/headers`; client write string contains `ss_consent=` and `samesite=lax` and does NOT contain `secure`.
    - `src/lib/site.ts` contains `established: 2024` (grep returns one match).
    - `bun run tsc --noEmit` exits 0 (no new type errors).
  </acceptance_criteria>
  <done>analytics.ts and consent.ts compile and export the named symbols; site.established present; track() guards prove no-op when gtag undefined.</done>
</task>

<task type="auto">
  <name>Task 2: Analytics component (consent-default stub + env-gated GA4 loader) + 4-locale consent copy</name>
  <files>src/components/Analytics.tsx, src/dictionaries/en.json, src/dictionaries/fr.json, src/dictionaries/es.json, src/dictionaries/ar.json</files>
  <read_first>
    - src/components/Analytics.tsx (new — does not exist)
    - src/components/JsonLd.tsx (analog: script injection into the tree — PATTERNS line 162)
    - src/app/[lang]/layout.tsx (analog: env-read-then-omit convention at lines 62-68 — verification env)
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md (AGENTS.md: verify next/script beforeInteractive/afterInteractive semantics for THIS build before asserting)
    - src/dictionaries/en.json (files being modified — Dictionary type derives from en.json per dictionary.ts:1-4; the cta namespace at en.json:36-44 is the placement analog)
    - .planning/phases/03-measurement-conversion/03-RESEARCH.md §1 (Code Examples), Pattern 1, Pattern 2
    - .planning/phases/03-measurement-conversion/03-UI-SPEC.md Copywriting Contract lines 265-272 (exact consent copy, 4 locales)
  </read_first>
  <action>
    Create `src/components/Analytics.tsx` exporting `Analytics`. Read `const gaId = process.env.NEXT_PUBLIC_GA_ID`; if unset `return null` (no-op build, D-01, mirrors the layout verification env-read at lines 62-68). Before interpolating, validate `gaId` matches the GA measurement-id shape (anchored alphanumeric-with-dash starting `G-`) and return null if it does not — prevents the inline-script XSS sink (RESEARCH Security Domain). Render two ordered `next/script` blocks per RESEARCH §1: (1) an inline stub at `strategy="beforeInteractive"` that defines `window.dataLayer`/`gtag` and pushes `gtag('consent','default',{ad_storage:'denied',ad_user_data:'denied',ad_personalization:'denied',analytics_storage:'denied'})`; (2) the GA4 loader `<Script src={gtag/js?id=gaId} strategy="afterInteractive">` plus an inline `afterInteractive` config script calling `gtag('js', ...)` and `gtag('config', gaId)`. Ordering (stub beforeInteractive, loader afterInteractive) is the load-bearing Consent Mode v2 gate. If this build rejects `beforeInteractive` in `[lang]/layout.tsx`, fall back to an inline stub `<script dangerouslySetInnerHTML>` placed ahead of the loader (RESEARCH A4 fallback) — verify against script.md first.
    Add a `consent` namespace to all 4 dictionaries with keys `body`, `accept`, `decline`, `ariaLabel`, using the EXACT copy from UI-SPEC lines 267-272 (en/fr/es/ar). Add to `en.json` first (the Dictionary type derives from it), then mirror the shape in fr/es/ar or TypeScript will error.
  </action>
  <verify>
    <automated>for f in en fr es ar; do node -e "const d=require('./src/dictionaries/$f.json'); if(!d.consent||!d.consent.accept||!d.consent.decline||!d.consent.body||!d.consent.ariaLabel){process.exit(1)}" && echo "$f consent ok" || echo "$f MISSING consent keys"; done; bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Analytics.tsx` returns null when `NEXT_PUBLIC_GA_ID` is unset (grep: `process.env.NEXT_PUBLIC_GA_ID` and a `return null` path).
    - `Analytics.tsx` source contains `strategy="beforeInteractive"` on the consent-default stub AND `analytics_storage:'denied'` (or `"denied"`), and `strategy="afterInteractive"` on the GA4 loader — the denied stub precedes the loader in source order.
    - `Analytics.tsx` validates the GA id against an anchored `^G-` pattern before interpolation.
    - All 4 dictionaries expose `consent.body`, `consent.accept`, `consent.decline`, `consent.ariaLabel` (the node check above prints "ok" for en/fr/es/ar).
    - `bun run tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Analytics renders nothing without a GA id; with a valid id it emits the denied-default stub before the GA loader; consent copy present in all 4 locales; types compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConsentBar (JS-mounted, native buttons) + mount Analytics/ConsentBar in [lang]/layout.tsx</name>
  <files>src/components/ConsentBar.tsx, src/components/ConsentBar.test.tsx, src/app/[lang]/layout.tsx</files>
  <read_first>
    - src/components/ConsentBar.tsx (new — does not exist)
    - src/components/LocaleSwitch.tsx (analog: "use client" + document.cookie write + JS-mounted conditional UI — cite line 1, 79-82; PATTERNS line 39-53)
    - src/components/Reveal.tsx (analog: useSyncExternalStore hydration gate so the bar is ABSENT from SSR HTML — cite lines 9-15; PATTERNS line 55-62)
    - src/components/ContactForm.tsx (analog: native-button visual class string + Pick<Dictionary,...> prop typing — cite line 13, 70; PATTERNS line 64-67, 72)
    - src/components/Button.tsx (reference ONLY — do NOT add onClick; copy variant class strings for native buttons, cite lines 8-10)
    - src/lib/analytics.ts (grantConsent — created in Task 1)
    - src/lib/consent.ts (writeConsent client / readConsent server — created in Task 1)
    - src/app/[lang]/layout.tsx (file being modified — mount points alongside Header/Footer/PopupHost at lines 94-97; cookie read body at line 76 area; env-read convention lines 62-68)
    - src/lib/session.ts (analog: cookies() import for the SSR read — cite line 2)
    - .planning/phases/03-measurement-conversion/03-UI-SPEC.md Surface 1 (lines 147-184), Interaction Contract (lines 318-325), SSR Verification Contract (line 390)
  </read_first>
  <behavior>
    - Given consentKnown=true (server saw a cookie) → ConsentBar renders nothing
    - Given consentKnown=false and not yet hydrated → renders nothing (absent from SSR HTML)
    - After hydration with consentKnown=false → renders the bar with Accept + Decline native buttons
    - Click Accept → writeConsent("granted") + grantConsent() called, bar unmounts
    - Click Decline → writeConsent("denied") called, grantConsent NOT called, bar unmounts
  </behavior>
  <action>
    Create `src/components/ConsentBar.tsx` as a `"use client"` component `ConsentBar({ dict, locale, consentKnown }: { dict: Pick<Dictionary,"consent">; locale: Locale; consentKnown: boolean })`. Use the `useSyncExternalStore` hydration gate from `Reveal.tsx:9-15` so the bar is NOT in SSR HTML (UI-SPEC line 155 / SSR Verification line 390): render `null` on server/first-paint and when `consentKnown` is true; render the bar only when hydrated AND `consentKnown === false` AND no local decision has been made. Track a local `decided` state so the bar unmounts immediately after a click. Bar shell: `fixed bottom-0 left-0 right-0 z-40 bg-espresso text-cream py-3 px-4` with a `max-w-7xl mx-auto` inner; mobile stacks copy-over-buttons, `md:` row with `justify-between` (UI-SPEC lines 159-172). Use native `<button type="button">` for Accept and Decline (NOT Button.tsx — UI-SPEC line 174): Accept gets the `variant="light"` class string from `Button.tsx:8` (`bg-cream text-espresso hover:bg-tan`), Decline gets the outline class string (`border border-current`, `Button.tsx:9-10`); both `min-h-[44px]` (44px tap target). Copy from `dict.consent.{body,accept,decline}`; set `aria-label={dict.consent.ariaLabel}` on the bar. On Accept: `writeConsent("granted")` then `grantConsent()` then set decided. On Decline: `writeConsent("denied")` then set decided (no grantConsent). No mount/unmount animation (UI-SPEC line 344; `prefers-reduced-motion` → zero motion via display toggle, not transitions). RTL is inherited from `<html dir>`; reverse button order via `flex-row-reverse` when `dirFor(locale)==="rtl"`.
    Create `src/components/ConsentBar.test.tsx` covering the five behaviors above (mock `grantConsent`/`writeConsent`; follow the existing component-test style under src/components/*.test.tsx — e.g. ContactForm/page tests).
    Modify `src/app/[lang]/layout.tsx`: import `cookies` from `next/headers` (same as session.ts:2) and `readConsent` from `@/lib/consent`; in the `RootLayout` async body (line 76 area) read `const consent = await readConsent()` → derive `consentKnown = consent !== undefined`. Mount, alongside the existing `<Header>/<Footer>/<PopupHost>` (lines 94-97): `<Analytics />`, `<ConsentBar dict={dict} locale={lang} consentKnown={consentKnown} />`. DO NOT mount TrustBand or StickyCtaBar here (that is plan 03-02, per-page only — D-09 / Pitfall 5).
  </action>
  <verify>
    <automated>bun run test src/components/ConsentBar.test.tsx 2>&1 | tail -20; grep -c "useSyncExternalStore" src/components/ConsentBar.tsx; grep -c "<Analytics" 'src/app/[lang]/layout.tsx'; grep -c "ConsentBar" 'src/app/[lang]/layout.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test src/components/ConsentBar.test.tsx` passes all five behavior cases.
    - `ConsentBar.tsx` uses `useSyncExternalStore` (hydration gate — grep returns >=1) and renders native `<button type="button">` (NOT the `Button` component) for Accept/Decline.
    - `ConsentBar.tsx` does NOT import `onClick`-bearing `Button` for the consent buttons (grep `<Button` returns 0 in ConsentBar.tsx).
    - `[lang]/layout.tsx` mounts `<Analytics` (grep >=1) and `<ConsentBar` (grep >=1) and reads consent via `readConsent`/`cookies()`.
    - `[lang]/layout.tsx` does NOT mount TrustBand or StickyCtaBar (grep for both returns 0).
    - Runtime SSR check (record for verification phase): `curl .../en/ | grep -i "consent"` does NOT return the bar markup; `bun run tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>ConsentBar tests pass; bar is JS-mounted (absent from SSR HTML), uses native buttons, fires grantConsent only on Accept; layout mounts Analytics + ConsentBar and SSR-reads ss_consent; TrustBand/StickyCtaBar NOT mounted in layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → inline script | `NEXT_PUBLIC_GA_ID` is interpolated into an inline GA config script (server-controlled value crossing into client script context) |
| client → GA4 | Event payloads leave the browser for Google's GA4 endpoint |
| browser → cookie | `ss_consent` written/read across requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/Injection | `Analytics.tsx` inline script | mitigate | Validate `NEXT_PUBLIC_GA_ID` against anchored `^G-[A-Z0-9-]+$` before interpolation; the consent stub is a static literal with no user-data interpolation |
| T-03-02 | Info Disclosure | `track()` payloads | mitigate | Helper sends only event name + non-PII params (`method:"contact_form"`); never form values, email, or phone (enforced in 03-02 call sites) |
| T-03-03 | Privacy (Law 25) | consent ordering | mitigate | `analytics_storage` default-denied stub runs `beforeInteractive` (before gtag.js); `grantConsent` only on explicit Accept; cookie-write failure leaves state denied (safe default) |
| T-03-04 | Tampering (build) | `NEXT_PUBLIC_GA_ID` env | accept | Public-by-design measurement ID; not a secret. Kept out of git for hygiene only (Dokploy-set). No code mitigation needed |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages (web-vitals/@next/bundle-analyzer install is in plan 03-03 with its human-verify checkpoints) |
</threat_model>

<verification>
- `bun run tsc --noEmit` exits 0 across the new lib + components.
- `bun run test src/components/ConsentBar.test.tsx` passes.
- Consent ordering: `Analytics.tsx` emits the denied-default stub before the GA loader in source order.
- 4-locale consent copy present (en/fr/es/ar node check).
- Runtime (verification phase, after deploy with a GA id set): GA4 DebugView shows NO hits before an Accept click; `analytics_storage` reads `granted` only after Accept.
- No-op build: with `NEXT_PUBLIC_GA_ID` unset, no `googletagmanager.com/gtag/js` appears in built HTML.
</verification>

<success_criteria>
Maps to ROADMAP Phase 3 success criterion 1 (MEAS-01): GA4 installed behind Consent Mode v2 default-denied; a 4-locale consent bar flips analytics on opt-in; the AI-referrer channel-group regex is drafted for the user to paste before the first new page ships (no backfill). Code no-ops safely when the GA id is unset.
</success_criteria>

<output>
Create `.planning/phases/03-measurement-conversion/03-01-SUMMARY.md` when done.
</output>